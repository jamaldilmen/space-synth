# 🎯 TODO — THE WHOLE OPEN LIST, IN FOUR BUCKETS

**This file is the cold start. Read this, not the boards.**
`BOARD.md` (94 KB) and `BOARD_BLACKHOLE.md` (84 KB) are the DETAIL — open one only for the row you are working.
`BOARD_CLOSED.md` is history and is never read at cold start.

**Body written:** 2026-08-20 14:08:59 · **Header re-stamped:** 2026-08-23 14:12:09 · **Commit:** `0cf85b4` (2026-08-23 14:40:21) — HEAD, pushed, but ⚠️ **14 UNCOMMITTED PATHS in the tree** (the whole colour session: `render.metal`, `spectral_lut.h`, `renderer.mm`, `main.cpp`, `app_state.h`, `window.{h,mm}`, the boards). *(Re-stamped 2026-08-25 14:25:10: this line said `a44de13` / “nothing unpushed”, which was one commit stale and silent about the working tree.)* *(Previously stamped `6b923fe`, which was three commits stale while the file already carried 2026-08-23 rows. The body's `file:line` citations were verified against `e853e18` and are unchanged — bumping the sha does NOT re-verify them.)* · **Bundle:** killtube `2026-08-23 12:15:13`, one second newer than the newest source file (`src/ui/window.mm` 12:15:12) — measured, not stale.

⭐ **Every `file:line` below was re-read in the source on 2026-08-20.** Line numbers are stamped, not permanent.
⭐ **The five rows stamped 2026-08-23 — S00, S0, S0b, A2, A7 — were verified this session at `a44de13`.** Every other row still carries its 2026-08-20 re-read and nothing more.
Rows that could NOT be checked without a run or a rebuild are listed at the bottom under **NOT VERIFIED TODAY** — do not treat those as facts.

**Four buckets, his call.** Physics rows are filed under the symptom he SEES, not under "physics".

**Three standing gates:** build with `bash package_macos.sh`, never bare `make` · launch `--env SS_FULLSCREEN=1` · never test the hole above **1×** time warp.

🎪 **THE SHOW — 2026-09-05, COLOGNE. IT IS A THREE-WALL ROOM, NOT A WALL: two sides 14.75 × 3.50 m + front 10.01 × 3.50 m = 39.5 × 3.5 m unwrapped, 138.25 m², 11.286:1, ~270° around the viewer.** ⭐ **Those are SCAN-MEASURED (Polycam OBJ, 2026-08-24) — row S00e, the settled geometry. Read S00e, then S00.** ⛔ **Every “15 × 4 m / 10 × 4 m / 160 m² / 10:1” in this file is SUPERSEDED** (his first message, 2026-08-23 12:26:22), and so is the 08-24 hand-measured **“140 m² / 11.429:1”** — the walls are **3.50 m**, the sides **14.75 m** (he said 15; 1.7% short), the front **10.01 m**. ⛔ **The old “~40 m², 10 × 4 m, 2.5:1” line is the FRONT WALL ONLY**, and every “2.5:1” elsewhere in this file means that one slice. 🚨 One camera cannot render 270° — three off-axis frustums from one eye position, never one wide image sliced up. Full working: **`docs/DESIGN_2026-08-23_THREE_WALL_ROOM.md`** (⚠️ written pre-scan; S00e overrides its numbers).
⚠️ **`docs/SHOW_2026-09-05_COLOGNE.md` is SUPERSEDED ON GEOMETRY** (its lines 9–11 still say ~40 m² / 10 × 4 m / 2.5:1, written 2026-08-21 before the room was known). Its non-geometry content — the show-driven rows S1–S6 and what an agent may and may not do — still stands. ⛔ **Set length, arrangement and the live/pre-rendered mix are HIS**, not ours.

## 🎪 SHOW ROWS CLOSED 2026-08-21

| # | What landed | Verified | Note |
|---|---|---|---|
| ~~**S1**~~ | ✅ **THE RENDER BUFFER IS PINNED TO EXACT PIXELS.** `SS_WIDTH`/`SS_HEIGHT` set the drawable itself, not the window. The window became a scaled preview. Projection aspect now reads the DRAWABLE (`main.cpp:791`, `:797`); `width()/height()` stay POINTS for ImGui — a deliberate, documented split. | `[DEPTHPREPASS] target 3840x1536` at 2.5000:1 with a 1656×662 pt preview, 2026-08-21 22:11:47 | 🚨 The first version was WRONG: it sized the WINDOW, so macOS clamped 2560×1024 to a **3600×2048** drawable — the wrong aspect, silently. His screenshot caught it |
| ~~**S2**~~ | ✅ **STAR SIZE IS RESOLUTION-NORMALISED, AS ONE LAW.** Every pixel quantity in `particle_vertex` is in REFERENCE pixels (a 2260-tall drawable, his fullscreen height, measured); ONE multiply at the tail converts to device pixels. | `render.metal:2614` · fullscreen no-op confirmed: meanPx 1.02 / maxPx 25.71 vs 25.73 before | 🚨 The first version scaled `rawSize` + `zoomCap` and left FIVE pixel laws unscaled. His order: *"we dont want two values for a single thing ever"* |
| **S6** | 🔴 **OPEN — NO CAPTURE PATH EXISTS.** `grep` for prores/AVAssetWriter/recordFrame/exportFrame over `src/` → **zero hits**. He plays a MIXED set (live + pre-rendered), so recordings are real deliverables. | verified 2026-08-21 | His fork: record the Syphon feed externally, or build an in-app export. **S1+S2 were the prerequisites and are now done** |

⚠️ **G2 is the lever, not S2.** ⛔ *(The "96–99% in the bottom bin" figure this line used to carry was a POST-clamp artifact — see the corrected G2 row. Real figure: 52.5% floored, meanRaw 1.079 px.)* MEASURED 2026-08-21: the post-clamp bin reads 96–99% at every resolution — the size law emits sub-pixel values and `clamp(rawSize, 1.0f, …)` (`render.metal:1552`) is doing all the work. The picture is a floor, not a law. That is the number under *"star size is still stars, not smear of stretched light"*.

⛔ **A retraction, logged:** the first S2 A/B ("8× the pixels → meanPx 1.02 vs 1.26") was **time-confounded** — the two runs were sampled at 12 s and 25 s. At a matched first sample both read **1.02**, which confirms the device-pixel diagnosis harder than the wrong numbers did. Compare KPROBE at matched elapsed points, never `tail`.

---

## 🕳️ BH — the priority (his order 2026-08-14: *"BH LENSE THEN TUBE / SPHERE BEFREIUNG"*)

| # | What is open | Verified today | Deciding move |
|---|---|---|---|
| **BH1** | **The whole 08-17 emission law is UNTESTED.** blackbody `T(r)`, `g`, and the absorption/transfer all landed; his verdict was *"dtill the same in every way lol"* — because the march only ever saw 3.7% of the disc. | `app_state.h:57` bit19 default ON | Get the field to the expanded-disc state (meanR ≈ 44 sim, r_s ≈ 0.23), then read **`[MARCH] bCull`** — never off a screenshot |
| **BH2** | 🚨 **TWO Ω LAWS FOR ONE DISC.** Sprite Doppler uses `1/(r^1.5 + KERR_A)` and a hardcoded global **+Z** tangent; the march uses `√(M/r³)` and `poseAxis`. U6/U7 unified the arcs and playback and never reached the sprite. | `render.metal:1610`, `:1611` vs `:3716`, `:3721` | One law, one axis. This is the unified-physics goal, in one function  ⚠️ **A fix was written and reverted 2026-08-20 14:22:25 on a misread verdict — it was never rejected on its merits. Re-applying is cheap.** |
| ~~**BH3**~~ | 🔨 Perspective divisor fixed 2026-08-20 (`renderer.mm`), still unverified by eye. 🆕 **METRIC SHADOW DEFAULT FLIPPED OFF 2026-08-22 06:56** — his words: *"turn the shadow off btw its fake and annoying"*. `app_state.h` `uiTogMetricShadow = false`. 🚨 **AND IT DID NOT REMOVE THE DARK CIRCLE.** The perfect black disc is a SEPARATE object: a depth-only analytic sphere at `b_c = 2.598 r_s`, `render.metal:3164` + `renderer.mm:3852`, gated ONLY on `lastHorizonR > 0` — **not on bit15**. It writes depth with colour masked off, which is what made the hole OCCLUDE (the 2026-08-14 "the hole is a body" fix). His words for it: *"the ring that still is the black hole without the lense"*. ⚖️ Killing it takes the body away again — **his call, not ours**. | verified in source 2026-08-22 07:35 | Decide the sphere before touching the lens further |
| **BH4** | ✅ **NOT OPEN — the board is wrong.** `BOARD_BLACKHOLE.md` §2 and §6 row 1 still list the parity pinch ring and the tuneLens-blind secondary as open. **Both were fixed 2026-08-14** (17:30:54 and 17:53:52) and the code says so at length. ⚠️ Consequence worth keeping: a lens A/B done with the **SLIDER** before that date is still void. | `render.metal:1164` (placed by the lens equation, no `mix`), `:1184` (`imageWeight = cam.tuneLens * lensRamp * …`) | Correct the BH board, do not re-do the work |
| **BH5** | **The lens is OFF whenever he plays.** `bhLensActive = totalAmplitude < 0.02f` — the hole only lenses at silence. Deliberate, but it means the show never sees it. | `renderer.mm:1662` | His call, not a bug |
| ~~**BH6**~~ | ✅ **FIXED 2026-08-22 04:53 — AMR MOVED TO bit21.** `renderer.mm:1962` + `particles.metal:2164`. **The bug was worse than the board said: `SS_NO_AMR` WAS A NO-OP.** bit15 was already set by `uiTogMetricShadow` (default ON), so `bhToggles \| (amrOn ? …)` never changed anything — every A/B run with that flag compared two IDENTICAL configurations. bit21 is free (bhToggles packs 0..20; bits 21+ elsewhere belong to `debugFlags`, a different word); no preset serializes bhToggles, so no migration. | A/B at matched resolution 2026-08-22 05:05: core M(<0.5) 2.935e4 (AMR off) vs 5.283e4 (on) at sample 4 — the flag now changes physics. ⚠️ n=1 per side | ⛔ **Every AMR conclusion in this project's history is VOID** — the switch never worked |
| **BH7** | **The 128³ box-average objection is unaddressed** — the march samples NEAREST from a 128³ grid, so it can never resolve sub-cell structure. | `app_state.h:57` | Structural (L4). Decide, don't drift |
| **BH8** | ✅ **b_c IS CORRECT — independently derived, not re-quoted.** `tools/bc_validate.cpp` now EXISTS (it did not) and I compiled and ran it: bisection on an independent null-geodesic integration gives **b_c = 5.196152422707 M** vs analytic `3√3·M` = 5.196152422707 — rel err **8.2e-15**; the escaping ray skims r_min = 3.0025 M against a photon sphere at 3M. The shipped shader constant **2.5980762** (`render.metal:3177`, `:937`, `:1104`) is correct to **4.4e-09**. ⚠️ Worth keeping: the shipped integrator's own step size is load-bearing — at its real step 0.03 it lands 2.59803855 (rel err 1.5e-05), but at step 0.10 that degrades to 1.1e-03. **Do not loosen the step.** 🔴 **STILL OPEN: gravitational redshift**, the `√(1−2M/r)` half of `g`. | run by me 2026-08-22 01:42 | Redshift only; b_c is settled |
| **BH9** | **The accretion rows** (moved into `BOARD_BLACKHOLE.md` §N2): **A2** reversibility now runnable · **A3①** `/0.5` denominator real but not binding · **A3②** hole profile centred on the ORIGIN, not the mass · **A5** the fuse is a 3–16 min stochastic wait (a show risk) · **A6** refund-floor leak · **A8** `feed` returned nonzero for the first time · **MERGER-FACE** a merger has no visual. | §N2 | Each is its own row |
| **BH10** | **His 12:13-run complaints, unactioned:** *"still a fake visual not physical overlay"* (⚠️ the §2 parity proof says the optics ARE real — so it is the BLEND that reads fake) and *"usually a black hole is reddish blueish, not just blue grey"*. | §4d | Screen time, one at a time |


---

## 🎬 THE SHOW — his brief 2026-08-23, **full text in `docs/DESIGN_2026-08-23_TRUEFX_AND_THE_SHOW.md`**

> ***"in audio they say every effect is either eq or delay. and i wanna use that analogy to create our TRUEFX (audio + visual fx) not two effects chained together. the same data drives the effect for sound and visuals."*** · ***"i want more disco… not nasa. we make a movie now. a show."*** · ***"these things NEED TO BE DONE ASAP cause the show is happening and its gonna be big."***

| # | What | State |
|---|---|---|
| **T0** | 🚨 **THE RULE: a TRUEFX is ONE effect with TWO OUTPUTS.** Same data drives sound and image. An audio block + a shader block sharing a knob is **NOT** a TRUEFX and he will reject it. Axis: **EQ-family removes/shapes light · DELAY-family repeats an image in time.** | governs T1–T4 |
| **T1** | **REVERB = the FLUIDITY mechanism.** His mapping: screen persistence IS the tail. Visual half already exists (`trailDecay`, "Fluidity"); the audio half does not. | half exists |
| **T2** | **DELAY — neither half exists.** *"delay we dont have yet."* | ⬜ not started |
| **T3** | **CHORUS = per-pixel PROPER chromatic aberration.** Audio `chorus_` exists; the visual half is the flat radial offset already open as a mixed-space bug (G10). Fixing G10 is step 1 of T3. | audio only |
| **T4** | 🆕 **LADDER LOWPASS = OUR OPACITY.** *"playing with the filter makes it darker. but not just darker like proper darker.. it filters it. not just on off. but the effect will be an opacity-like fader. to automate."* ⚠️ **"Opacity-like" describes the CONTROL (one smooth automatable fader), NOT the implementation.** A cheap `alpha *= x` is exactly the "just on off" he is ruling out — the image must lose **spatial high frequencies** (detail, sparkle, edges first) as cutoff drops. | ⬜ not started |
| **T5** | 🪩 **DISCO.** *"shiny like disco ball surfaces to appear at one point in the sim. like sparkly shiny."* ⚠️ A **direction change** on the project's visual thesis — but *"not nasa"* is about the LOOK, **not** permission to fake the sim. It is an EVENT that appears and passes, not a permanent surface. | ⬜ direction set |
| **S00** | ⚠️ **SUPERSEDED ON NUMBERS BY S00e (scan, 2026-08-24) — read that row first.** The **14.75 / 10.01 / 3.50 m, 138.25 m², 11.286:1** figures there are measured; every number in THIS row (15 m sides, 140 m², 11.429:1) is hand-measured or from his first message and is kept only for the reasoning. ⭐ **What survives S00 unchanged is the STRUCTURAL argument — one camera cannot render 270°** — which no scan changes. 🚨🎪 **THE VENUE IS A ROOM, NOT A WALL — 2026-08-23 12:26:22. Full working: `docs/DESIGN_2026-08-23_THREE_WALL_ROOM.md`.** *"3 walls in a room, 2 at 4m x 15m and one front… let's say 10 m… i need to think in three slices."* ⛔ **THOSE FIRST NUMBERS WERE WRONG — HE WALKED THE VENUE 2026-08-24 and the walls are 3.5 m high, not 4.** Corrected and recomputed: two sides **15 × 3.5 m (4.286:1, 52.5 m² each)**, front **10 × 3.5 m (2.857:1, 35 m²)**, unwrapped **40 × 3.5 m = 140 m², 11.429:1**. *(Superseded: 15×4 / 10×4 / 160 m² / 10:1, from his message 2026-08-23 12:26:22.)* ⭐ **The front wall is almost exactly a 4K frame laid on its side:** at 3840 px wide it is **384 px/m** and needs **3840 × 1344** for square pixels — his *"the front wall is basically a 4k screen"*, confirmed by arithmetic. ⛔ **EVERY "2.5:1" ON THIS BOARD IS NOW ONLY THE FRONT WALL** — today's 3840×1536 pin is one slice, not the show. 🚨 **THE STRUCTURAL FACT: ONE CAMERA CANNOT RENDER THIS, and it is arithmetic not taste.** Three walls wrap ~270° around a viewer inside; a flat perspective needs width ∝ tan(fov/2), which is 11.4 at 170° and **infinite at 180°**. So rendering one wide 10:1 image and slicing it is WRONG — fine mid-front, catastrophic at the corners. **Correct construction = ONE camera POSITION, THREE OFF-AXIS FRUSTUMS** (CAVE / planetarium): build an asymmetric projection from the shared eye to each wall rectangle's corners. Corners then agree with no seam. ⭐ This is also exactly what R1's *"driving a little space ship through our universe"* requires. 💰 **Budget at 4K/wall: 3.93+3.93+5.90 = 13.8 MP/frame, 2.33× today's fill** — and today measured *"1 zu 1"* at 2.5× the previous fill, so fill is probably not the wall. 🚨 **The real cost is GEOMETRY: three frustums = `particle_vertex` running 30 M times per frame.** Never measured under that load. Promotes per-wall frustum culling and G12 index compaction. ⭐ Already banked: the corpse discard at `render.metal:839` fires 71 lines in, so dead matter is already cheap. ❓ **ASK THE TECH: 4K PER WALL or 4K TOTAL?** — 256 px/m vs **96 px/m**, a factor of 3, and 4K-total is visibly chunky. Also: three feeds or one wide feed? (Syphon is currently ONE server, `renderer.mm:418`.) ⚠️ **Star size becomes a CORRECTNESS bug here**: it is in DEVICE PIXELS and never normalised, so the same star is a different PHYSICAL size on a side wall than on the front. | ⚖️ Answers from the venue tech 2026-08-24, then pin three slices  *(evidence: his brief + arithmetic 2026-08-23 12:26:22)* |
| **S00b** | 🚨🧍 **THE ROOM HAS PEOPLE IN IT — his venue visit 2026-08-24 14:31:16.** *"people are gonna b there so the bottom m is basically off for anything that needs people to see it. its gonna be super packed too so the height of people matters. the top of the front wall is the main visual field.. the top half of it. so if something needs to be in focus it needs to be there."* **MEASURED CONSEQUENCE, all from 3.5 m:** the bottom **1.0 m is dead** — occluded by a packed standing crowd — leaving a usable band of **1.0…3.5 m = 2.5 m, 71% of the wall height**. Usable front becomes **10 × 2.5 = 4.000:1**; usable side **15 × 2.5 = 6.000:1**. 🎯 **THE FOCUS BAND is the top half of the front wall: 1.75…3.5 m, 10 m wide = 5.714:1.** 🚨 **That band is 12.5% of the room's total 140 m².** Anything that must be READ — structure, the hole, a moment — lives in that 12.5%. The other **87.5% is peripheral vision by construction.** ⛔ **This kills any composition that centres the subject vertically:** a centred hole sits at 1.75 m, i.e. exactly on the crowd's sightline edge, and its lower half is in the dead metre. Vertical framing must be biased UP. | ⬜ Bias the front-wall camera up; treat the 5.714:1 band as the real canvas  *(evidence: his eyes + arithmetic 2026-08-24 14:31:16)* |
| **S00c** | 🧘 **IT IS A MINDFULNESS OPENING, NOT A SHOW — his order 2026-08-24 14:31:16:** *"i think the full on universe explosion thing on all three sides is a bit wild for a mindfulness space opening lol. we need super slow rides and work in a way that respects the room."* ⛔ **This retires the "universe explosion" framing for Cologne.** The 08-23 brief's camera rides stay, but slow. ⭐ **And it agrees with the geometry rather than fighting it:** 87.5% of the surface is peripheral, and peripheral vision is high on motion sensitivity and low on acuity — fast wide motion out there reads as agitation, which is the opposite of the brief. Slow large-scale drift on the sides, detail only in the front focus band. ⚠️ **NOT a measured claim from this repo** — that is standard visual-system behaviour, flagged as reasoning so it is not later cited as our measurement. | ⬜ Pace every camera ride against this before building any of them  *(evidence: his order 2026-08-24 14:31:16)* |
| **S00d** | 📐 **THE SIDES CANNOT HAVE THE FRONT'S SHARPNESS ON ONE 4K FEED EACH — computed 2026-08-24 14:31:16.** Front at 3840 px over 10 m = **384 px/m**. A 15 m side given one 4K feed is **256 px/m — 1.50× softer**, and the softness CHANGES AT THE CORNER, which is where it is most visible. To hold 384 px/m a side needs **5760 × 1344 (7.74 MP)**; all three then total **20.64 MP/frame = 3.50× today's 3840×1536 pin**. 🚨 **Fill is probably not the wall — measured 2026-08-23, 2.5× the fill cost nothing (*"1 zu 1"*) — but GEOMETRY is untested at this load:** three frustums means `particle_vertex` runs 30 M+ times per frame. ❓ **ASK THE TECH:** how many feeds, what resolution each, and is edge blend the media server's job. | ⚖️ Venue tech answers, then pin three slices  *(evidence: arithmetic 2026-08-24 14:31:16)* |
| **S00e** | ✅📐 **THE 3D SCAN CONFIRMS HIS TAPE MEASURE — measured 2026-08-24 21:4x from `~/Downloads/8_24_2026.zip`** (Polycam OBJ, 256,532 verts / 419,221 tris / 46.8 MB; extracted analysis only, the mesh is NOT in the repo). **Floor plane y = −1.50** (118 m² captured, the dominant horizontal). **Wall tops y = +2.00.** ⭐ **HEIGHT = 3.50 m EXACTLY** — his hand figure, confirmed independently. **Footprint X −5.00…+5.00 = 10.00 m, Z −7.25…+7.50 = 14.75 m.** So the front wall measures **10.01 m** (he said *"10 ish"*) and the sides **14.75 m** (he said 15 — **1.7% short**, keep 14.75 for pixel maths). **REVISED SHOW NUMBERS:** sides **14.75 × 3.5 = 51.6 m²** each, front **35.0 m²**, total **138.25 m²** (not 140), unwrapped **39.5 × 3.5 = 11.286:1**. Focus band **17.5 m² = 12.66%** of the room. At 384 px/m: front **3840×1344**, side **5664×1344**, total **20.39 MP**. ⭐⭐ **THE THREE SHOW WALLS ARE CLEAN — this is the good news.** Surface present in a 0.25 m grid: Side A **98%**, Side B **98%**, Front **96%**, and **the top 0.25 m band is present across 100% of the front wall's width.** 🎯 **Nothing interrupts the focus band.** No pillars or columns anywhere in the projection volume. 🚪 **The BACK wall (Z=−7.25, behind the audience) has a 1.25 m opening at x −5.00…−3.75** — the entrance, back-left corner. Not a projection surface; it is where the crowd enters and therefore where density will be highest. ✅ **THE OBJECT WAS A PERSON — his answer 2026-08-24 22:0x:** *"yeah it was a person the room is still being setup it was just about getting the scale correct."* Not an obstruction. **No shadow risk on the focus band.** ⭐ **AND THE SCAN'S PROVISIONAL CONTENTS DO NOT MATTER:** everything taken from it is the SHELL — floor plane, wall planes, wall tops, openings. The shell is the part that will not change while the room is dressed. Nothing on this board depends on where the furniture was on 2026-08-24. 🚪 **THE BACK WALL GETS NO BEAMER — his confirmation 2026-08-24 22:0x:** *"the side with the door the entrance doesnt get a beamer. so its as ive stated before only the sides and the front."* Three projection surfaces, exactly as first stated: **Side A 14.75 × 3.5, Side B 14.75 × 3.5, Front 10.01 × 3.5.** The back wall is crowd-flow only. | ✅ **CLOSED — the geometry is settled.** Everything downstream can now be built on 14.75 / 10.01 / 3.50  *(evidence: scan-measured 2026-08-24, both open questions answered by him 2026-08-24 22:0x)* |
| **S0** | 🆕🚨 **RESOLUTION + THE PERF HEADROOM IT REVEALED — measured 2026-08-23 12:08:16.** He had been running the panel at **1920×1200 @1x** while it can do **3024×1964** — only **39% of his pixels**. Set to native. Then pinned the render to **3840×1536 (2.5:1, 5.9 MP)**, the wall's format. ⭐⭐ **HIS VERDICT: *"it looks fucking amazing"* AND *"es läuft halt 1 zu 1 so wie vorher"* — 2.5× the fill rate at 10M particles for NO measurable frame cost.** That is the single most useful perf datapoint this project has: **we are not fill-rate bound at 5.9 MP.** 🎪 **HE NEEDS MIN 4K FOR THE SHOW** and gets exact pixel specs from the venue tech **2026-08-24** — until then 3840×1536 is the working assumption. ⚠️ Also confirms the earlier camera-feel complaint was RESOLUTION, not the camera: star size is in DEVICE PIXELS and is never normalised to the drawable, so halving the pixel count doubles how large a star reads. | Await the venue's exact numbers, then re-pin  *(evidence: measured + his eyes 2026-08-23 12:08:16)* |
| **S0b** | ✅ **UI SCALE FIXED 2026-08-23 12:08:16** — his: *"alles ist jetzt voll klein auf meinem screen"*. Cause: font size and style metrics both hung off `backingScaleFactor`, which is **1.0 in every 1× mode** and therefore says the same thing at 110 ppi and at 255 ppi. Now derived from **physical DPI** (`CGDisplayScreenSize`): measured **255 ppi → 2.32×**. `SS_UI_SCALE=<0.5..4.0>` overrides. ⭐ **RE-MEASURED 2026-08-23 14:47:03 and the source now agrees** (it had said 253 ppi in three comments): the panel is **255.0 ppi physical** — 3024 px across 301.21 mm = 11.859 in. 🚨 **`CGDisplayPixelsWide` returns the CURRENT MODE's width in POINTS, not physical pixels**, so the function computes **points-per-inch**, not dpi: 255.0 in a 1× mode, **127.5 in a 2× mode**. That is correct and must not be "fixed" — ImGui multiplies the backing scale in afterwards, so 2.32 × 1.0 and 1.16 × 2.0 both land on the same physical size. The old comment called it dpi, which is only true at 1×. Backing scale still does its real job — glyph crispness — which is a separate axis and was being conflated with UI size. | ⬜ a live +/- keybind would beat a relaunch  *(evidence: measured 2026-08-23 12:08:16)* |
| **S0c** | 🆕🎪 **CINEMATIC SHAPE MUST NOT APPLY ON THE LAPTOP — his order 2026-08-23 20:12:26:** *"new version needs to automatically adapt to full screen or not. it cant be cinematic on a macbook screen thats stupid. or let me toggle it."* **Today the render shape is fixed at LAUNCH and never changes:** `main.cpp:136` pins the drawable only `if (getenv("SS_WIDTH") \|\| getenv("SS_HEIGHT"))`, `window.mm:482-483` stores it, `window.mm:393-396` sets `layer.drawableSize` once at create. **There is no runtime path to re-pin — none.** The window is only a preview of that buffer (`window.mm:318-337` fits it to 92% of the visible frame and prints the scale), and since today it **letterboxes** rather than stretches (`window.mm:391`, `kCAGravityResizeAspect`). ⭐ **So "cinematic on a MacBook" is literally black bars**, which is exactly what he is objecting to. **WHAT IS NEEDED:** one runtime toggle between **SHOW SHAPE** (pinned wall format, preview letterboxed) and **SCREEN SHAPE** (drawable follows the window, no bars). 🚨 **This is NOT a variable flip.** The postfx chain and the Syphon feed are sized from the drawable, so re-pinning at runtime is a **resource rebuild**. ⚠️ **NOT YET VERIFIED BY ME:** how many resources hang off drawable size and whether a resize path already exists — `main.cpp:117-119` cites `renderer.mm:1151` and `:4515` for this, and **a comment is not a mechanism**; that must be read in source before any estimate. 🚨 **HARD CONSTRAINT:** whatever the preview does, **the Syphon feed and any recording must stay at SHOW shape**, or a capture made while he is working windowed is wrong permanently — that is the whole reason S1 pinned the buffer in the first place. | ⬜ **Read the drawable-resize path in `renderer.mm` FIRST**, then price it. Do not start until the BH/colour work is off the bench  *(evidence: his order 2026-08-23 20:12:26; current state source-verified same day)* |
| **S1–S4** | 🚨⛔ **ID COLLISION — READ THIS FIRST (caught 2026-08-25 15:11:3x): the `S1` and `S2` in THIS row are NOT the `~~S1~~`/`~~S2~~` two tables up (L24–25), which are ✅ CLOSED (render buffer pinned; star size resolution-normalised). Those are a different numbering. So S6's note *“S1+S2 were the prerequisites and are now done”* refers to the CLOSED pair and says NOTHING about Ableton Link or MPE — both of which are ⬜ NOT STARTED.** 🚨 **SHOW INFRA, he marked URGENT** — **Ableton Link · MPE** (named twice) **· recording/render route · EVERY PARAMETER A MACRO reachable by MIDI CC.** He will sequence the whole 30-min set in Ableton and *"automate every single fucking thing."* ⭐ **S4 gates the rest** — an un-addressable knob cannot be in the set. Collides with the standing row that **star dials do not exist at all**; same fix. | ⬜ URGENT |
| **R1** | 🎥 **THE CAMERA RIDES** — *"super smooth cinematic rides and pans… feel of driving a little space ship through our universe. AUTOMATED. proper rides… as in gta."* Plus a **CINEMATIC MODE button**: *"zoom and tilts and time warp become super slowed down.. like cinematic mode on my dji."* ⭐ **The board ALREADY has the mechanism for the second half** — the second-order `SmoothDamp` camera row sitting on his verdict IS cinematic mode. Read it as that. ❓ He also asked a real research question: *"how do cameras in sims like that work?"* | ⬜ |
| **L1** | 🔴 **LIFECYCLE — MEASURED (⚠️ DATE LOST: this row shipped with a literal `%s` placeholder that was never substituted, introduced in `a44de13` 2026-08-23 10:49:36 and caught 2026-08-25 15:11:0x; the row carries no other date, so the measurement date cannot be recovered — do NOT invent one), HE IS RIGHT AND IT IS ONE LINE.** *"midi length needs to dictate how long stuff is… at 10ms arp the sim kinda gets stuck."* `envelope.cpp` Release: `relDur = clamp(sustainHeld, params.release, 1.5f)` — `params.release` (**0.400 s**, `envelope.h:22`) is the **LOWER** clamp, so **every note gets a ≥400 ms tail no matter how short**. A 10 ms note → ~400 ms of visual, **40× the note**. At a 10 ms arp ~40 voices overlap, all in Release; `getDominantEnvelope()` ranks Attack highest so the phase the GPU sees **pins to Attack and never resolves**, and `intensity` (a SUM) saturates. **That is the "stuck".** ⚠️ Second defect: `sustainHeld = envTime` is called "hold time" but `envTime` **resets on Attack→Decay**, so it measures from a moving origin. | 🔧 fix ready, not applied |

---

## 🎨 GRAPHICS

| # | What is open | Verified today | Deciding move |
|---|---|---|---|
| **G1** | 🚨 **NO PER-PARTICLE DEPTH IN THE STAR PASS.** The depth **pre-pass** writes depth (`depthWriteEnabled = YES`), but the additive particle pass is still write-OFF, so nothing orders or shades by distance. This is the one fact under "it doesn't read as 3D", and it blocks correct camera blur and per-particle motion vectors outright. | `renderer.mm:1103` (NO, particle pass) vs `:1116` (YES, pre-pass) — **re-stamped 2026-08-20 19:03:44**, the L1 lens fix pushed both down 9 lines from the `:1112`/`:1125` printed at 14:08:59 | Design first — turning writes on is not one line (store action, buffer, blend order) |
| **G2** | **Star size — his biggest remaining look complaint:** *"star size is still stars, not smear of stretched light"*. 🚨 **THE 96% FIGURE IS WRONG — CORRECTED 2026-08-22 09:52 BY DIRECT MEASUREMENT.** That number was read from the POST-clamp histogram, whose bin 3 spans [0.92, 1.41) and so merges the floored stars (sitting at exactly 1.0) with the ones that naturally land 1.0–1.4. Two populations, one bin. **A pre-clamp probe now exists** (`[KPROBE-RAW]`, `render.metal` in the every-64th gate, slots 84–102): **meanRaw = 1.079 px · FLOORED = 52.5% · capped = 0.0%**, distribution 0.59:49.0% 0.92:39.2% 1.41:7.3% 2.18:3.4% 3.36:0.9% 5.19:0.1%. ⭐ **So the floor touches HALF the field, not 96% — and it is not the real problem.** Unclamped, the typical star is still ~1 px. Removing the clamp yields sub-pixel dots, not smear. ⛔ **`zoomCap` NEVER BINDS (0.0% capped)** — that branch and the `fluxComp` brightness compensation hanging off it are DEAD in this configuration; do not tune them. | measured 2026-08-22 09:52, n=25533 | A 1-px honest output cannot be stretched by a size dial. Size + streak as ONE law, or a PSF whose width is an instrument property rather than geometry |
| **G3** | **⛔ THE RIBBON PASS IS DELETED — 2026-08-20.** `TrajOut`, `trajectory_vertex`, `trajectory_fragment`, its pipeline and its draw call, ~370 lines. His verdict on the thickness test: *"this provers the trail theory dead ... its the wrong approach"*, then *"delete the fucking code and put it 6 feet under"*. **Do not rebuild it.** The diffraction-cross/"diamondy" complaint outlives it and belongs to the sprite. | `render.metal` headstone at the old site; code in git at `5ee213d` | — |
| **G4** | 🆕 **THE GYROSCOPE IS SOLVED — 2026-08-22.** His words: *"the axis is lose its turnign wihtin itself"*, then the correct recall: *"the thing with the rings came when we unified the orbits ... we had two systems at work made it 1 since then its fucked."* **He was right and he named the commit's intent from memory.** `render.metal` playback read `float3 axis = poseAxis(rel)` from **2026-08-15** ("EVERY STAR ON ITS OWN ORBITAL PLANE", his order then) — a DIFFERENT axis per star, tilted by that star's own `atan(\|z\|/ρ)`, so stars at different heights sheared the field into nested tilted rings. **Fixed 2026-08-22 08:20: one axis for the field.** +Z is DERIVED, not magic — the launch law `v = ẑ × r` (`particles.cpp:256`) puts total L along +Z by construction. ⭐ The 08-15 order no longer binds: it existed to match the ARC RIBBONS, which were deleted 08-20. | his verdict 2026-08-22 08:40:10: **"looking better"** | ⚠️ Two things this did NOT fix, below (G4a/G4b) |
| **G5** | **Extinction FAILED and the reason is physics, not render** (his eyes: *"still a rick and morty eye"*). The absorption is live and at full strength, but the clump is ~1 cell across while the march steps 1.5 cells — the first sample is already outside it. | `render.metal:2590-2591` | Density must resolve better than 1.0 sim. A render fix cannot land this |
| **G6** | ⚠️ **PARTLY CORRECTED 2026-08-22 22:02:55 — "per-particle motion vectors are not started" WAS WRONG.** They exist: `render.metal:2687-2689` `float2 velocity [[color(1)]]`, star pass writes `RG16Float`, blending off (`renderer.mm:695-696`, `:3702-3705`). Depth is stored too (`depthPrepassTexture`, `:3830-3836`). **Still true:** the camera blur itself is disabled and unprojects a hardcoded `z = 0.99` far-plane proxy instead of reading that depth. 🔴 **The real remaining gap is narrow: only the star pass writes velocity** — `writeMask = MTLColorWriteMaskNone` at `renderer.mm:746`, `:777`, `:814`, `:841` kills it on the march, background and disk. 🚨 **G14 gates this** — the matrices that produce screen motion are the ones read 4 bytes out of place. | source-verified 2026-08-22 22:02:55; **re-verified in full 2026-08-26 09:0x** | 🔴 **STILL OPEN, AND IT IS NOT A ONE-LINE FIX. ⛔ MY OWN 2026-08-25 NOTE — “turn on the four writeMasks” — WAS WRONG AND IS RETRACTED HERE.** ⭐ **First, the good news: the consumer is LIVE.** The velocity texture is NOT orphaned. It is read by **the SMEAR** at `postfx.metal:274` (`motionTex.sample(...).rg * u.smearShutter * u.pixelStretch`), inside the block gated at `:264` on the two dials — **not** by the dead camera blur at `:499` (`if (false && velLen > 0.002)`), which is a SEPARATE effect. *(I first reported motionTex as never-sampled; that was a bad grep, corrected same session.)* ⚠️ **The smear is invisible at rest:** `pixelStretch = clamp(spinMag / kSpinMax)` (`main.cpp:2401`), so it is **0 until SPIN is up**. Any verdict on this row needs spin turned up. 🚨 **THE BLOCKER, and it is the whole row: THE THREE FRAGMENT SHADERS CANNOT WRITE A VELOCITY.** `hole_fragment` (`render.metal:3080`), `bhmarch_fragment` (`:3342`) and `dust_fragment` (`:3166`) each return a plain **`float4`** — one colour output, no `[[color(1)]]` anywhere. Only `particle_fragment` returns `ParticleFragOut` (`:2735-2738`, `float2 velocity [[color(1)]]`). **So flipping a writeMask does not turn velocity ON — it lets UNDEFINED data into attachment 1.** The mask is the LAST step, not the first. 💬 **7th sighting of “a comment is not a mechanism”:** all four masks are commented *“this draw is not moving matter”*, which is false for the march (flowing gas) and the disk (rotating) — but that comment being wrong still does not make the mask flippable. ⛔ **AND IT IS THREE PASSES, NOT FOUR.** `:747` = `holePipeline`, `:778` = `bhMarchPipeline`, `:842` = `dustPipeline`, but **`:815` = `bhBodyPipeline`, the DEPTH-ONLY capture sphere** (which is why `:817` masks colour entirely). Invisible geometry must never write motion vectors, or the smear drags colour along a sphere nobody can see. **Leave `:815` and `:817` alone.** 🛤️ **REAL ORDER OF WORK:** (1) give `hole_fragment` and `dust_fragment` a `ParticleFragOut`-style velocity output — they are point sprites, so the star pass's method ports directly, and note it projects through the **CURRENT** viewProjection at both ends (`:2733-2734`) so camera motion cancels; (2) **`bhmarch_fragment` is a DESIGN PROBLEM, not a port** — a volumetric ray-march has no per-particle identity, so its velocity must come from the gas flow field; decide or park it; (3) only then flip the corresponding masks, one pass at a time so a bad smear is attributable; (4) separately, `postfx.metal:478` still unprojects a hardcoded `z = 0.99` while real depth exists in `depthPrepassTexture`, and `:499`'s camera blur stays off for a real reason recorded inline (it averaged HDR glow with tonemapped SDR and /4, so camera motion DIMMED the glow). |
| **G7** | **THE SMEAR — four attempts, all rejected 2026-08-20.** Screen-space motion smear now exists and is wired to REAL motion (star pass writes a velocity target; camera cancels because both ends use one matrix). Dials: Smear length, Smear hold. 🚨 **KNOWN BUG, DIAGNOSED NOT FIXED:** 48 taps over a several-hundred-pixel band puts samples 5–8 px apart, so each star repeats as separate beads and MORE length makes it WORSE. The fix is a multi-pass doubling smear, not more taps. | `postfx.metal`, the smear block | Build the multi-pass version, or park it |
| **G8** | **Poisson SOR ≈ 6 ms — double the whole star pass.** 80 sweeps × 2 colours = 160 compute encoders per frame, with a live knob. | `renderer.mm:2433-2438`, `SS_SOR_SWEEPS` | Needs an **accuracy** verdict, not a perf one |
| **G9** | **Chladni sharpness** — *"almoooost."* `ridgePull` uses the SCULPT gradient, not the eigenmode ∇Ψ, and there is no node dissipation. **The eigenmode itself is genuinely 3D — do not re-litigate that.** 🆕 **Second, separate defect on the same feature — verified in source 2026-08-20 19:03:44:** `modes.h:11` declares `alpha` as "Bessel zero value J_m(alpha)=0", but `modes.cpp:24` assigns `440.0 * pow(2.0, (midi-69)/12.0)` — the note's frequency **in Hz** — under a comment calling it "mostly vestigial in Phase 9". It is **not** vestigial: `main.cpp:694` copies it into voice data and `bessel.cpp:42` evaluates `besselJ(m, alpha * r)`. ~~A 440 Hz note therefore evaluates J_m at argument 440·r instead of at a Bessel zero.~~ 🚨 **THAT CONSEQUENCE IS REFUTED — agent-verified 2026-08-22.** `bessel.cpp`'s `Z2()` and `potential()` have **ZERO callers** repo-wide; the file compiles but is dead, so the Hz value never reaches a Bessel evaluation. The ONE live reader is `particles.metal:2476`, which uses `alpha` as a **scalar gain** in a `cos(mθ)sin(nφ)` sculpt (no Bessel call), gated behind bit16 — which `main.cpp:2468` leaves OFF by default. **In the default config `alpha` affects nothing.** The GPU already documents the bug at `particles.metal:532-536` and looks the true zero up itself at `:2519`. ⚠️ `main.cpp:694` is a STALE citation — the real copy is `:716`, plus two unlisted producers at `:665` and `:683`. ⛔ **Do not book this as a visual fix**: the live path never read the broken value, so it cannot touch the *"almoooost"* complaint. It is a data-truthfulness fix (3 files — the CPU zero table is 7×4 but `modes.cpp` emits m=0..11, n=1..9, so it must be extended to 12×9 to match the live GPU table). 🆕 Live inconsistency found on the way: under `SS_SCULPT=1`, VJ-mode sculpt is ~100× weaker than MIDI-mode, because `main.cpp:585-600` already feeds real Bessel zeros into the same expression MIDI feeds Hz into. ⚠️ Do not conflate with the `ridgePull` half above. | `modes.cpp:24` · `modes.h:11` · `main.cpp:694` · `bessel.cpp:42` | Swap the gradient source |
| **G10** | ✅ **SCANLINES DELETED 2026-08-22 17:44:00 — his order: *"forget scanlines delkte it"*.** Gone from all 8 sites; `postfx.metal:500` is now a headstone carrying the measurement, and the uniform slot was **kept as `postPad0` deliberately** (see G14). Built and verified: `strings` on the bundle → **0** hits for "scanline". **Why it could not be saved:** at a fragment centre the argument was exactly `π(row+0.5)`, so `sin` returned `(−1)^row` — `line` was exactly 0 or 1, **never between** (0 of H rows in (0.01,0.99), H = 1080/1440/2160/2234/1537). 0.5000 cyc/px, **f/f_N = 1.0000**. ⛔ A prefilter cannot fix a pattern on Nyquist (area-average still keeps 0.6366), so **no softness dial was ever possible**. Cost 30% of the light at full dial. 🔴 **THE CA HALF IS STILL OPEN** — `postfx.metal:169-176`. ⛔ Do **not** call it "elliptical": the direction is correct (the pixel offset is parallel to the pixel radius). The defect is a **MIXED SPACE** — a pixel-space vector times a **UV-space** radius, so `\|P\|/R²` spreads **×1.000 square · ×1.547 at 3456×2234 · ×2.500 at 2.5:1**; h/v comes out **W/H** where r² wants **(W/H)²** (2.50 vs 6.25 on the wall). One line: aspect-correct `dist`. Dial defaults to 0 (`app_state.h:92`). | deleted + built + verified 2026-08-22 17:44:00 | CA space fix is **S** and independent — his call |
| **G11** | **Rick-and-morty eyes** — start from his name for it. ⛔ My bit20 theory is dead. Do not re-pitch it. | C11 | — |
| **G12** | ⛔ **THE "CHEAPER WIN" IS REFUTED — 2026-08-22 22:29:24. THE HOIST ALREADY EXISTS.** The row claimed `particle_vertex` reads the mass at `render.metal:768` but does not discard corpses until `:1889`, so *"~1120 lines of lensing/Doppler/blackbody run for every corpse"*. **Verified false in source by me:** there is a full corpse discard at **`render.metal:839`** — `if (mass < 0.001f) { …writes all 11 varyings…; return out; }` — **71 lines after the mass read, at function-body scope** (brace-depth checked), and `mass`/`in.posW.w` are **never reassigned** between `:768` and `:1889`. A corpse has `posW.w` zeroed by the merge kernel, so it returns at `:839`. The `:1889` block tests `in.posW.w <= 0.001f`, so the ONLY value that can reach it is **exactly 0.001f** — it is redundant dead code, not the discard site. Those 71 lines are the horizon cull (`:783`) and secondary-image cull (`:809`), **both gated on `mass > 0.001f`**, so a corpse skips both bodies and pays two failed compares. **There is nothing to hoist.** 🚨 **AND THE OLD A/B NEVER TESTED THIS:** `SS_NO_DEADSKIP` gates `particles.metal:1220` — the **COMPUTE** kernel — not the vertex shader. The board conflated two different shaders. ✅ **What survives from this row: INDEX COMPACTION**, unaffected by any of the above, and the `BOARD.md:409` blocker correction. | source-verified by me 2026-08-22 22:29:24 | Index compaction only. ⛔ Do not spend a session on the hoist |

| **G4a** | 🔴 **THE SPAWN GIVES A SPHERE A PLANAR VELOCITY LAW.** `particles.cpp:256-262` gives EVERY particle `v = ẑ × r` — purely azimuthal about +Z, `vz = 0` — while the HALO (15% of the field, Plummer a=15) is spawned ISOTROPICALLY on a sphere (`:189-199`). Speed is `v_circ(r3)` from the 3D radius but the lever arm is `lxy`. Off-plane particles are therefore NOT on circular orbits; each sits on an inclined great circle set by its birth `z`. ⚠️ **LATENT SINCE 2026-07-20 (`1bb9c70`) — a month old, NOT the recent regression.** He was right that the visible break was new; this is the older condition underneath it. Also: particles within 1e-4 of the Z axis fail the `if` and get **zero** velocity — they fall radially through the centre. | `git blame` 2026-08-22 08:12 | Velocity perpendicular to its OWN radius, not to Z — changes the whole initial condition, **his call** |
| **G4b** | 🔴 **THE HOLE HAS NO INTAKE — IT HAS A DELETE KEY.** A merge does not move matter inward: `particles.metal:3792` sets mass→0 and teleports the corpse to `park = 4000 + id%1024` (r ≈ 6928) — 🚨 **CORRECTED 2026-08-22: that park is ONE FRAME, not a grave.** `particles.metal:1161` ESCAPER RECYCLE (`r_curr > 1000`) re-enters the corpse **at its star-map home on a Kepler orbit** the next frame. It is back in the field, interleaved with the living. What holds it dead is the write-back gate `if (mass > 0.0f)` (`particles.metal:3502`) — it never writes state again, and its mass was handed to the eater and is never refunded. **The conclusion survives — nothing falls in — but my first mechanism for it was wrong.** Nothing falls in, so there is no accretion flow to render. Revival fires ONLY during sustain (`envelopePhase` 2.5–3.5); the rest-trickle was cut 2026-08-04 to stop a mass pump. **MEASURED this run: `live=999 → live=2` of 1000, `insideRS=0`** — the field ate itself to 0.2% and the centre held no live matter. What survives is what the hole could never reach. His words: *"the merger to bh state is broken. only works at launch with the fake drag."* | `[PROBE-1000]` progression, 2026-08-22 07:15 | This is the row under "the flow of the bh doesnt make sense" |
| **G4c** | ⚠️ **THE VISIBLE MOTION IS PLAYBACK, NOT PHYSICS — and it stops when he plays.** The rotation is gated `envelopePhase < 0.5` (silence only) and `rxy > horizonR`. Raw physics is **~38 s/orbit** by the block's own comment. So a played note switches the time-lapse OFF and the field looks frozen. His words: *"the mergers jsut sit there. its so weird. physics dead here."* — correct observation, inverted cause: the life was the time-lapse. | `render.metal:674` gate | Not a bug yet. Decide whether play should keep turning |

| **G13** | 🆕 **`entanglement.z/.w` ARE DOUBLE-BOOKED** — star-map θ/φ **and** bond-target/selfOrig. ESCAPER RECYCLE reads θ/φ out of those words (`particles.metal:1163-1165`) to compute the corpse's re-entry home, so **bonding corrupts the recycle destination**. Separately, `packForGPU` and `Renderer::resetParticles()` write two incompatible conventions into the same words. ⚠️ Found by the G12 agent OUTSIDE its scope and NOT verified by me beyond the recycle read — treat the second half as unconfirmed. | agent 2026-08-22 | Second double-booking found this week (bit15 was the first) |
| **G14** | ✅ **FIXED 2026-08-22 22:29:24 — and now COMPILER-BOUND, not comment-bound.** `PostFXUniforms` was 4 bytes out of sync (MSL 240/matrices at 112; C++ 236/at 108) because 27 scalars = 108 B and only MSL pads to the `float4x4`'s 16-byte alignment. **Fix: one `float postPad1;` in both mirrors → 28 scalars = 112 B**, so both sides land the matrices identically WITHOUT relying on either compiler's implicit padding. ⭐ **Added the guard this struct never had**, in the project's own existing `CameraUniforms` style (`renderer.h:333-345`): `sizeof == 240` plus `__builtin_offsetof` anchors at `chromaticAmount == 16`, `smearHold == 104`, `inverseViewProj == 112`, written identically into `renderer.h` and `postfx.metal`. **PROVEN BOTH WAYS:** the real asserts compile, and the old broken values (236 / 108) are confirmed to **FAIL** the Metal compile — the guard has teeth. C++ side re-measured 240 B / offset 112. | built + verified 2026-08-22 22:29:24 | ✅ done — G6 is no longer gated |
| **G15** | 🆕🎨 **THE HUBBLE AND JWST BAND SETS WERE ALREADY IN THE CODE AND HAD NEVER BEEN SELECTED — found 2026-08-23, wired 2026-08-24.** His order: *"we should have james webb and hubble as basis i told ya … not true to my liking but true to the science and the gold standards: nasa"*. `spectral_lut.h` has carried `kBandVisible`, `kBandHubble` and `kBandNircam` since 2026-07-24. `renderer.mm:974` read `const BandSet &bs = kBandVisible;   // §4.2 default; switching rebakes` — **no UI, no env var, no caller ever switched it.** 💬 **6th sighting: a comment is not a mechanism.** ⭐ **MEASURED with the header's own math 2026-08-23 20:12:26** (normalised to the brightest channel, spread = max−min across B/G/R, higher = more colour): **visible** 2000 K 0.955 · 5772 K 0.332 · 10000 K **0.173** · 30000 K 0.447 — 🚨 **the hot end COLLAPSES; a blue giant renders nearly grey, and that is the band set, not the tonemap.** **hubble** 0.980 · 0.126 · 0.548 · **0.765** — separates both ends, 1.7× the blue at 30000 K, near-white Sun (correct above the atmosphere). **nircam** 0.569 · 0.956 · 0.973 · 0.982 — ⛔ **NOT a drop-in: in 0.8–5 µm everything above ~3000 K is on the Rayleigh–Jeans tail, so B=1.000 / G=0.10–0.20 / R=0.02–0.04 for EVERY star from the Sun up — the whole starfield becomes one blue.** JWST images are colourful from EMISSION LINES and dust, which is the lines LUT, not the continuum. NIRCam is the gas story. **SHIPPED:** `SS_BANDS=visible\|hubble\|nircam`, **default hubble**, `renderer.mm:974-985` + `bandSetByName()` `spectral_lut.h:100`. Built and verified in the binary (`strings` shows `SS_BANDS`, bundle 2026-08-23 20:17:15 > newest source 20:17:06). 🚨 **KNOWN CONSEQUENCE, NOT YET JUDGED BY HIM: the Hubble edges move the emission lines between channels.** Hβ 0.4861 and [OIII] 0.5007 both fall in B (0.400–0.550) and Hα 0.6563 falls in G (0.550–0.700), so **R gets nothing**: the launch self-test prints `lines g=1.00 B/G/R weight (4.00,2.86,0.00)` against an `expect (1.00,3.00,2.86)` string that is **hardcoded for the visible set** and is now comparing against the wrong thing. Under `visible` the lines landed one per band, Hα→R — which is what makes nebulae read red. **Stars improved; the gas lost its red.** ✅ **RESOLVED 2026-08-24.** His verdict: *"its oakly witht he colors but nt 0 perfect yet"* — so hubble stayed, and the gas defect was fixed by measurement rather than by reverting. **The line positions FIX two band edges** (B\|G between Hβ 0.4861 and [OIII] 0.5007 → 0.495; G\|R between [OIII] and Hα 0.6563 → 0.600), leaving only the outer edges free. Swept them against a stated objective — Sun near-white, hot end separated, cool end deep red — and **`kBandStellar` "stellar-bvr" B 0.360–0.495 / G 0.495–0.600 / R 0.600–0.720 beats BOTH shipped sets**: 2000 K 0.962 · 5772 K 0.175 · 10000 K **0.636** · 30000 K **0.810**, lines one per band. (hubble was 0.548/0.765 with lines COLLAPSED; visible 0.173/0.447.) Those edges sit close to Johnson **B/V/R**, the standard stellar-photometry system. **DEFAULT since 2026-08-24**, `SS_BANDS=stellar\|visible\|hubble\|nircam`. ✅ **The hardcoded `expect` string is FIXED** — `expectedLineBands()` now derives it from the live band set (`spectral_lut.h`), so the self-test validates whatever is shipped instead of one set's answer. Both self-tests pass: `(1.00,3.00,2.86)` and `[one line per band]`. | measured + built + launch-verified 2026-08-24 22:01:23; his verdict 2026-08-24 | ✅ Closed. Colour quality now lives in G16 |
| **G16** | 🔴🎨 **THE ORANGE FIELD IS A LOCAL DEFECT, NOT THE COLOUR LAW — MEASURED FROM A SCREENSHOT 2026-08-24 22:04.** *(Method: `screencapture -x` → ffmpeg → raw RGB → histogram. See [[feedback_take_your_own_screenshots]].)* Whole frame: **92.5% of lit pixels red or orange, blue 2.26%, white 0.47%** — his *"not 0 perfect yet"*, quantified. 🚨 **Binning by RADIUS is what located it:** disk r<70 **92.0% warm / 2.8% cool** · ring 70–120 **96.9% / 1.7%** · outer 120–200 85.1% / 14.8% · **field r>200 62.4% warm / 37.6% cool (blue 35%, red 33%, orange 29%)**. ⭐ **Out in the field the colour is CORRECT and well spread. The collapse is confined to where the gas is dense.** So the band set, the Kelvin law and the tonemap are all cleared — the defect is local. ✅ **SUSPECT CLEARED BY MEASUREMENT:** the Shakura–Sunyaev disk override (`render.metal:2103`) repaints hue with `blackbodyRGB` and was the obvious culprit — but it is gated on `cam.horizonR > 0.0f` and the live log reads `horizonR raw=0.0000 smooth=0.0000`. **It never runs.** Checked before blaming it. 🔎 **LEADING CAUSE \[HYPOTHESIS — NOT YET PROVEN]:** the gas emission-line component. `lineStrength` is driven by trilinear gas DENSITY (`render.metal:2078`), so it fires exactly where the disk is dense and nowhere in the field — matching the radial split. Under Case B the line triplet is (B 1.00, G 3.00, R 2.86): normalised that is **G≈R with B at 0.33** — yellow-green, which over a red stellar continuum lands on the observed orange. ⭐ `spectral_lut.h` **already warns in writing that [OIII]/Hβ = 3.0 is not a constant** — it is an ionisation diagnostic. That ratio is the dial. | measured 2026-08-24 22:04 (n=1 frame, 45,748 lit px); cause is HYPOTHESIS | ⬜ **MEASURE:** one build with `lineStrength` env-gated to 0, re-shoot, re-histogram. If the ring's warm fraction drops, it is proven |
| **G17** | ✅🌈 **PHASE VIZ NO LONGER REPLACES THE COLOUR — his order 2026-08-24: *"also th ephase hingy needs to be stadnard on"*, and his choice when shown the options: BLEND, DON'T REPLACE.** **Was:** `render.metal:1775` `if (cam.phaseViz > 0.5f) { out.color = hsv2rgb(...); } else { …everything… }` — switching it on made the **entire blackbody + spectral path dead code** and the field a flat HSV rainbow, contradicting the standing directive 60 lines below it (*"ONE universal law … no ramps, no per-phase palettes"*, 2026-06-14). **Now:** the physical path ALWAYS runs; the tint is applied **once, late**, after both the play path (`:1866`) and the star-map mix (`:2322`) have written `out.color`. 🎯 **That single site is also the Chladni/starfield unification he asked for** — one effect, one place, both regimes identical, instead of two copies that drift. `uiPhaseViz` **default true**, new `uiPhaseVizAmount` **0.35** with a `Phase Amount` slider (right-click resets); `cam.phaseViz` is now an AMOUNT 0..1, not a 0/1 switch. 🚨 **HUE ONLY — Lupton holds.** S and V are carried through untouched by a true hue rotation on the circle (`rgb2hsv` added). | built + launch-verified 2026-08-24 22:47:31; his verdict *"looking ok"* 2026-08-25 | ✅ Closed. *(The “can't see the slider” worry was G18 — settled 2026-08-25 14:28:38: it renders, its LABEL is clipped by the panel width. Nothing wrong with G17.)* |
| **G18** | ❓ **HE CANNOT FIND THE `Phase Amount` SLIDER — his words 2026-08-25: *"looking ok dont see the slider tho"*. UNRESOLVED.** What is verified: the label **is** in the shipped binary (`strings` → 1 hit), it is drawn by the plain `UiSliderFloat` wrapper (`main.cpp:37`, no gating), and it sits at `main.cpp:1377` inside `if (ImGui::CollapsingHeader("SIMULATION", ImGuiTreeNodeFlags_DefaultOpen))` (`:1364`) — a LIVE header, not one of the `if (false && …)` dead blocks. ⛔ **My attempt to find it myself FAILED:** the crop I took captured my own terminal, not the app window. **Do not conclude it renders — that has not been seen.** 🚩 Precedent that makes this worth taking seriously: **A7** — the whole VJ panel is `if (false && …)` and its controls have been invisible since 2026-06-26. | ✅ **SEEN ON SCREEN 2026-08-25 14:28:38** — `screencapture` after `open -a` to raise the fullscreen Space | ✅ **CLOSED — the slider WAS ALWAYS THERE. The premise of this row was wrong.** It renders directly under the `Phase Viz` checkbox reading **0.35**, exactly where `main.cpp:1377` puts it. 🔎 **THE REAL DEFECT IS THE LABEL: it is CLIPPED BY THE PANEL WIDTH.** ImGui draws a slider's label to the RIGHT of the widget, and `PHYSICS ARCHITECT` is too narrow, so it reads **“Phase”** and “ Amount” is cut off — he saw an unnamed number box, not a Phase Amount slider. ⛔ **NOT specific to this control — the whole panel is clipping:** “Black Hole — mechanism”, “Star sc”, “Amoun”, “1 star = 1 R☉ (Sun) \| range 0.13” are all truncated at the same right edge, and a vertical scrollbar is present. ⚠️ **His call 2026-08-25 14:29: LEAVE IT — *“okay nvm its cool mov eon”*.** Not fixed, deliberately. The panel-width clipping stays open as a UI row for whenever the UI track is picked up. |

---

## 🔊 AUDIO

| # | What is open | Verified today | Deciding move |
|---|---|---|---|
| **A1** | **Sonification is at ZERO lines.** `grep -rl "sonif\|perParticleVoice\|fieldVoice" src/` → **no files**. The design is complete; nothing is built. | verified 2026-08-20 | D7: the per-particle voice at **N = 1**, the solo path |
| **A2** | ✅ **THREE RT-THREAD FIXES IN, 2026-08-23 09:32:47 — but see the correction on the third.** ① The pulse `fprintf(stderr,…)` inside `audioOutputCallback` — **deleted. This one was genuinely live**: that callback runs every buffer, always. ② `std::vector<float> windowed(fftSize_)` allocating 8 KB per FFT — **preallocated** into `Impl` beside `window`/`inputBuffer_`. **Also genuinely live** (runs off the input callback). ③ `getVJBands()` took `lock_guard(vjMutex_)` and copy-constructed its return value inside it, while `audioInputCallback:149` blocked on the same mutex. Replaced with a **seqlock** — `vjBands_` is now RT-private, the RT thread publishes wait-free, and the return type is `std::array<VJBand,16>` by value (stack aggregate, no malloc, both call sites unchanged). 🚨 **CORRECTION, same session — I ranked ③ as "the real dropout risk" and that was WRONG.** Both callers are **unreachable**: `main.cpp:1802` is `if (false && ImGui::CollapsingHeader("VJ MODE & AUDIO INPUT"` (*"removed 2026-06-26"*), the only checkbox that sets `uiVJMode` lives **inside that dead block**, and nothing else in `src/` sets it (`app_state.h:117` defaults false). So the main thread never took that mutex and the RT side never contended it. ③ is **insurance, not a fix to anything audible today.** ✅ Net true statement: `audio_engine.mm` now holds **zero locks** (grep-verified). 🔴 **STILL OPEN, `synth.cpp`, untouched and genuinely reachable:** blocking `lock_guard(queueMutex_)` `:91` vs the MIDI thread holding it across `std::sort` + reallocating `push_back` (`:161-171`); `voices_.erase()` at `:124` **inside the per-sample loop** and `:193`; `voices_[midi] = v` `:237`. ⛔ Do NOT naive-try_lock the exit requeue (`:137-141`) — breaks the swap invariant, drops notes. | built + verified 2026-08-23 09:32:47 | `synth.cpp` is the only audio work left that is on a LIVE path |
| **A3** | **Ceiling already measured:** N ≈ **250,000** voices at a safe 10% GPU share, 500k at 25%, **2M is not feasible**. Disk spans 3.9–4.3 octaves natively. | D3 / D5 | Design to 250k, not to 2M |
| **A4** | **The release still jumps.** *"when i hold longer it still kinda jumps to 0 gravity / star mode before the actual shape settled"* — the A4 ramp fixed the boundary it targeted; this is a different one. Structural: sound = stagecraft in the middle of the note, real physics at both ends. | note-lifecycle audit §2 | Support decaying to zero, so no branch is crossed |
| **A5** | **"The held shape has never been perfect"** — named by him, never actioned. | — | His to define |
| **A6** | **ETERNAL-ECHO** — *"an infinite echo of our self-oscillation"*. His ask, **explicitly low priority**. | — | Park until he raises it |
| **A7** | 🆕 **VJ MODE IS DEAD CODE IN A VJ INSTRUMENT — found 2026-08-23 09:32:47.** `main.cpp:1802`: `if (false && ImGui::CollapsingHeader("VJ MODE & AUDIO INPUT"` , commented *"removed 2026-06-26"*. The panel does not render, so **the Live Spectrum, the Input Gain slider and the "Enable VJ Mode (Mic/System In)" checkbox are all off-screen.** `uiVJMode` defaults false (`app_state.h:117`) and **nothing anywhere else in `src/` sets it**, so mic / system-in driving the visuals is unreachable by any route — no keybind, no env var, no preset. The consuming code is all still live and compiled: `main.cpp:579` (16-band harmonic sculpting), `:2558`, `:2626`, plus the whole FFT + onset-detection path that runs on the audio thread **every frame regardless**. ⭐ **So the analysis is being computed and thrown away.** ⚖️ **His call: is this wanted back for Cologne, or was it deliberately buried?** Two months dead with no complaint suggests buried — but a VJ instrument that cannot take audio in is worth one explicit decision rather than drift. | source-verified by me 2026-08-23 09:32:47 | ⚖️ **Ask, do not rebuild.** If wanted: flip `false`, then verify the checkbox path end-to-end |

---

## 🖥️ UI

| # | What is open | Verified today | Deciding move |
|---|---|---|---|
| **U1** | **E5 is built, uncommitted and UNSEEN** — his "groundwork" call (*"static info in a ui is stupid"*). `src/ui/` is **clean** in git, so the live panel is not there; it needs one look before anything else in this bucket. | `git status src/ui` clean | One pass at the screen |
| **U2** | ✅ **DERIVED 2026-08-22, agent + verified by me.** Accent **`#306CFF`** from **2400 K**, contrast **4.55:1** over void. ⭐ **The flip is not invented — it already exists in the code:** the LUT is baked in generator order (B,G,R) and `render.metal:382` swizzles to (R,G,B) for the field; the UI reads the SAME row **without** the swizzle. One transpose apart, provably derived. The live law is a real Planck band integral (`spectral_lut.h:37-55`, n=800 trapezoid, bands B .435-.500 / G .500-.590 / R .590-.700 µm at `:64-65`), max-channel normalised at `render.metal:2089`; bit16 DEFAULT ON (`app_state.h:63`). The Tanner-Helland fit at `render.metal:198` is the INACTIVE fallback and is substantially bluer. 🚨 **THE PANEL HAS NO FIXED BACKGROUND:** `ui_theme.h:29` sets WindowBg alpha **0.65** (verified by me) and postfx bleaches dense cores to white — so the accent is **4.55 over void but 1.44 over a lit field**. Only `#ECEEF3` passes AA on both. ⛔ **NO WARM UI COLOUR IS DERIVABLE FROM THIS LOCUS** — the agent built two warm tints, MEASURED them at 0.007/0.014 from the locus (i.e. lifts in disguise) and discarded them. The rule enforced itself. 🆕 `main.cpp:982`'s amber `#FF9933` is already a star (0.038 from the locus ≈ 3351 K); `ui_theme.h:48` "Electric Indigo" is a PICKED colour driving SliderGrab/ButtonActive (verified by me). | agent 2026-08-22, 3 claims re-verified by me | The 1.44-over-field ratio is the real problem, not the hex |
| **U3** | **4-level limit ladder** (yellow→orange→red→purple), numeric typeface as its own role: tabular fixed-width for numbers, proportional for prose. | E3 | — |
| **U4** | **The accuracy meter is shaky when the black hole is there** — his words, uninvestigated. | 4d.3 | — |
| **U5** | ⚠️ **THIS ROW MAY BE WRONG — CHECK BEFORE TRUSTING IT.** It says indigo hover states mean you are looking at an ORPHAN bundle. But **indigo is in the LIVE theme**: `ui_theme.h:48` `ImVec4(0.40f, 0.50f, 1.00f, 1.00f)` "Electric Indigo" drives SliderGrab / SliderGrabActive / ButtonActive (verified in source by me 2026-08-22). Either the trap note is stale or it means a different state. **Do not use indigo as a staleness signal until this is settled** — the bundle-stamp check is the reliable one either way. | verified by me 2026-08-22 10:05 | Settle or delete the row |

---

## ⚠️ NOT VERIFIED — needs a run or a rebuild, do not quote as fact

*(Shrunk 2026-08-22 by source-read verification. Everything settled below has left this block.)*

- **fps figures.** Last baseline ~31–36 fps idle @2M, worst frame 50–99 ms, march-live median 39.9 (n=776) — old bundles. **A starved run is not evidence.** 🆕 One fresh single sample: **30 fps at 2M at 3840×1536** (2026-08-21 22:11), a just-started run — a sample, not a verdict.
- **B5 — the −280 M☉ residual.** ⚠️ **Split verdict:** the MECHANISM is source-confirmed and still true; the MAGNITUDE needs a run and is contested. Do not quote the number.
- **32 build warnings → zero** (C10). The count still needs a rebuild. But the source half is settled below.

### ✅ LEFT THE BLOCK — settled by source read 2026-08-22, verified by me

| Row | Verdict | Re-stamped |
|---|---|---|
| **B2** radial cutoff | **STILL TRUE**, both halves — the window is origin-nailed | `particles.metal:405` (was `:300`, now a bare `//`), guard `:4288` (was `:3814`), centre `:4282` |
| **B3** bit4 origin-pin | **STILL TRUE** — spring live, ships OFF, only 3 enable paths | `particles.metal:1349`; `app_state.h:48`; `main.cpp:292`, `:1361`, `:2295` |
| **B10** density pressure | **STILL TRUE** verbatim — TODO comment and scale-12 literal both intact | `particles.metal:974`; comment `:969-973`; scale `:994` |
| **C10** source half | The warning is **REAL and PRESENT**; the `:485` citation is **DEAD** (that line is now `//`) | `render.metal:614` (`particle_vertex` decl) + `:626` (the `kProbe` param) |
| **`ssDiskTempShape`** | 🚨 **CORRECTION — it is NOT "still live".** It appears exactly ONCE in the whole tree: its own definition. **Zero call sites.** It is an *uncalled hook, deliberately retained.* The warning stands, but as **DO NOT SWEEP**, not "it is in use" | `render.metal:257` |
| **`renderer.mm:3894`** | **NOT FOUND — deleted outright 2026-08-20**, not relocated. Retire the citation; the dead road itself stands | headstones only: `renderer.mm:3961-3981`, `render.metal:2893-2899` |

### 🧭 A PATTERN, not a coincidence — **THE FACTS HOLD, THE ADDRESSES ROT**
Five independent checks on 2026-08-22 each found a decayed citation: `main.cpp:694→716` (G9), `render.metal:1621→1722` (BH4), `particles.metal:300→405` and `:3814→4288` (B2), `render.metal:485→614` (C10), `renderer.mm:3894→gone`. **Not one row was wrong about the code; nearly every row was wrong about where it lives.** ⭐ Re-read the line before citing it, always — and never trust a `file:line` older than the last refactor.

## ⛔ DEAD ROADS — do not retry (the short list)

- **A stroke per star, in any form** — 1 px lines (hair), then real triangle strips with a thickness slider
  (slabs). Four fixes were tried on it across 2026-08-20 (width conservation, luminosity, plane, thickness) and
  the look survived none. **Deleted 2026-08-20.** A million separate strokes with gaps between them IS hair.
- **The playback / time-lapse rate as a speed for anything physical.** It is ~150 rad/s of display speed. It
  blew up the Doppler once (logged), and driving the smear with it produced a screen-filling spirograph
  (2026-08-20 14:48). If a pass needs "how fast is this going", it needs the physics velocity or a geodesic law.
- **Dimming individual strokes to fix a mat of strokes.** Treats each hair as the thing to fix. His verdict:
  *"your apporach is wrong."*

- **postfx as the cause of anything** — ruled out on fps and on star appearance. Never suggest again.
- **Raising the march emission gain** to make the pass visible — screen-filling brown blob, reverted the same day.
- **Reading the marched region's size off a screenshot** — wrong twice, in both directions. Read `[MARCH]`.
- **The march as the hole's renderer** · **the fullscreen geodesic paint** · **the seed billboard** · **re-centring the lens on `cam.bhX/Y/Z`** (4 no-ops logged).
- **`L̂ = r × v` for the orbit normal** — use `poseAxis`, from position.
- **The 32-per-cell scatter cap is NOT dead code** — it is load-bearing. Writing past it creates mass.

---

**Last Updated:** 2026-08-26 10:34:14  *(G6 RE-VERIFIED IN FULL, no code touched. **My 2026-08-25 “turn on the four writeMasks” is RETRACTED.** The velocity consumer is live — it is the SMEAR (`postfx.metal:264`/`:274`), not the dead camera blur at `:499` — but the three fragment shaders (`hole_fragment`, `bhmarch_fragment`, `dust_fragment`) all return a plain `float4` with no `[[color(1)]]`, so flipping a mask admits UNDEFINED data rather than turning velocity on. It is also THREE passes not four: `:815` is the depth-only `bhBodyPipeline`. And the smear reads 0 at rest — `pixelStretch` ramps with SPIN. Real order of work now written into the row.)*
**Previously:** 2026-08-25 15:12:00  *(BOARD AUDIT, no code touched. **G6 UNBLOCKED** — its “fix G14 first” is stale, G14 closed 2026-08-22; its four writeMask lines drifted +1 (747/778/815/842) and `:817` is a fifth MaskNone that must NOT be flipped. **8 venue rows (S00–S0c) had 4 columns against a 3-column header**, so markdown was dropping their State cell and showing the evidence cell instead — S00e was rendering “scan-measured…” in place of “✅ CLOSED”; evidence folded into State. **`S1`/`S2` ID COLLISION** flagged on the S1–S4 row: the closed pair ≠ the URGENT show-infra pair. **L1 shipped a literal `%s`** where its measurement date belonged (from `a44de13`); date is unrecoverable, marked as lost. **One G18 cell I wrote earlier contained a bare `|`** that broke its row; escaped. Preflight board-size threshold raised 40 KB → 120 KB in `~/.claude/skills/handoff/preflight.sh` on his order — measured first: only 6,471 B of BOARD.md is closed-marked, and BOARD_BLACKHOLE §N only LOOKS like an archive.)*
**Previously:** 2026-08-25 14:25:10  *(DOC CORRECTIONS ONLY, no code touched: the cold-start show header carried the superseded 15×4 / 10×4 / 160 m² numbers while S00e says 14.75 / 10.01 / 3.50 m and 138.25 m² — fixed, and S00 now points at S00e; the header commit stamp said `a44de13` / “nothing unpushed” — now `0cf85b4` with the 14 uncommitted paths named; both boards dated `0cf85b4` to 08-24 when `git log` says 08-23; BOARD.md's live-worktree row still read `a44de13`.)*
**Previously:** 2026-08-25 13:45:06  *(Colour session: G15 closed — `stellar-bvr` measured and shipped as default, self-test expectation now derived from the live band set. G16 opened — the orange field located to the dense-gas region by a radial histogram, S–S disk override CLEARED by log. G17 — phase tint blends instead of replacing, one site, which also unifies Chladni and the starfield. G18 — he cannot see the new slider, unresolved.)*
**Previously:** 2026-08-24 18:08:07  *(Venue visit: S00 geometry corrected to 3.5 m — 140 m², 11.429:1 — and S00b/S00c/S00d added for the crowd dead metre, the mindfulness pacing order and the side-wall resolution mismatch. G15 added: the Hubble/NIRCam band sets were already in the source and had never been selected. Four broken table rows repaired.)*
**Previously:** 2026-08-23 14:12:09  *(header re-stamped to `a44de13` after it was caught three commits stale; bundle timestamp re-measured; the show line corrected from one 2.5:1 wall to the three-wall room, and `SHOW_2026-09-05_COLOGNE.md` marked superseded on geometry. No row body was touched.)*
**Previously:** 2026-08-22 10:10:00  *(agent sweep folded: U2 derived, G12's blocker corrected, G2's 96% figure retracted, the corpse-park mechanism corrected)*
**Previously:** 2026-08-22 08:45:00  *(gyroscope SOLVED and its two deeper rows opened; BH6 fixed; shadow default off; depth-sphere discovery)*
**Previously:** 2026-08-22 01:52:10  *(agent results folded in AFTER I re-verified each load-bearing claim in source; BH6 and G9 consequences CORRECTED, not transcribed)*
**Previously:** 2026-08-21 22:14:30  *(S1/S2 closed, show moved to Cologne; the 08-20 body is unchanged)*
**Previously:** 2026-08-20 19:03:44  *(body written 14:08:59; G1 line numbers re-stamped and the alpha-is-Hz defect added to G9 after a source re-read)*
