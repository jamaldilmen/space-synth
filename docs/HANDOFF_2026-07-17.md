# HANDOFF 2026-07-17 — the accretion combo is a SCALE problem, fix it with real matter

_Written 2026-07-17 21:20:00. Read `docs/BH_DISK_AUDIT_2026-07-17.md` first — this
is the action plan built on that audit._

## Jamal's decision (the direction, verbatim intent)
> "weve built a universe dude. we just have to get the numbers right as they
> clearly are not, and i've been voicing that we need actual matter not a fake
> layover forever."

**FORK B is chosen, framed as a numbers fix, NOT weeks of new physics and NOT a
render overlay.** The universe already produces real matter, real gravity, a real
honest horizon. The disk and the hole just live at the WRONG RELATIVE SCALE. Get
the scale numbers right so real matter populates the accretion region — never a
fake layover.

## The one root cause (from the audit, measured)
Nothing dwells in the accretion region r ≈ 1–4 sim (the ISCO/photon-sphere scale).
BAND probe: `r<0.4: ~410 | r=0.4–2.0: EMPTY | r>2: ~555`. Bimodal with a hollow
gap exactly where the disk must hug the shadow.
- Disk parked at r≈5–6 sim (from R_DISK=18) = **~3× the ISCO (~2 sim)** → a ring
  around a dot, not a disk hugging a shadow.
- Nucleus (support 0.55) plunges through r~1–3 into the hole, eaten, never dwells.
- Scale anchors: `kRsSimPerMsun ≈ 1.6827e-6`; r_h ≈ 0.5 sim; ISCO=3·r_s≈2 sim;
  shadow=2.6·r_s≈1.7 sim; disk≈5–6 sim.

## The numbers to get right (the actual work — all EXISTING knobs, no new systems)
Target: **the disk's settled radius ≈ the ISCO ≈ a few r_s.** Two families of knob,
both already in the code; tune, measure with the BAND/SHAPE probes, his eyes judge.

1. **Shrink the disk to the hole** — `R_DISK` (particles.cpp, currently 18) and the
   collapse target. Bring the disk's settled radius from ~5–6 down toward ~2 (the
   ISCO). Watch: does it then feed the hole and hug the shadow, or just get eaten?
2. **Grow the hole to the disk** — the accreted mass / `kRsSimPerMsun` so r_s≈2,
   ISCO≈6, matching where the disk already sits. Jamal wants the mass→r_s anchor
   physically correct, so prefer feeding the hole MORE real mass (nucleus fraction,
   infall rate) over changing the constant. Watch the two-mismatched-centers issue
   (centerGM=gmSim(4.297e6) at renderer.mm:1457 vs honest r_h≈0.5 — reconcile these).
3. **Make the nucleus DWELL, not plunge** — raise nucleus support (0.55) so it
   circularizes into a fed annulus at the ISCO instead of radial-plunging through.
   This is what actually PUTS real matter in the empty region. Measure: BAND r=0.4–2
   must stop being empty; SHELLV inner vr must go from +0.019 (bounce) toward ~0.

## The one render fix common to all (highest leverage, do early)
The shadow is drawn wrong: `hole_vertex` (render.metal) culls `r < r_h = r_s` and
draws SPARSE black splats. The visible shadow should be **2.6·r_s (Bardeen photon
capture), a SOLID disc.** That is the "tiny black blob / rick-and-morty eye." Fix =
draw a solid shadow at 2.6·r_s. This is honest (it IS the photon-capture size of
the real matter's r_s), not an overlay — it's sized by the same honest r_h.

## What shipped this session (in the commit being pushed)
- CIC deposit anti-alias (lines bug fix, already committed eb03c16 earlier).
- One-way horizon MEMBRANE: matter inside r_h is causally dead (physics + SPH +
  render), particles ARE the hole.
- Deflection MAP: exact Schwarzschild α(b/r_s) 256-LUT (renderer.mm), world-space
  per-particle lens in particle_vertex. **Verified correct vs published GR**
  (α(200)=0.0101, α(10)=0.237, α(2.60)=6.81). Can't show its wrap yet — the
  small-b accretion region is empty (the root cause above). It will light up the
  moment real matter reaches r~1–3.
- Two-era time axis, real relativistic playback dilation (rsDil=r_h, floor 0.02),
  plate-plane alignment, Doppler plane fix, flux-conserving far-field point sources
  (fps 26→54 zoomed out), hole-pass overdraw cap (deep pile 1/16).
- **Two MISSES (kept in the diff, honestly labeled):** disk support 0.95→0.99 and
  m=1 warp kill — both were one-line guesses at the two-ring shape; probes proved
  neither moved it (warp-off SHAPE trend identical to warp-on). Revert freely if
  the character is wanted back; they're orthogonal to the scale fix.

## Open ledger (unchanged, still real)
- COLLAPSE % denominator bug (Field 1.89e5 vs real ~1.1e6 → "COLLAPSE 135%").
- play↔BH regime (2D cross after play, speed-cap saturation, fps crash).
- eaten-particles-never-return: membrane is permanent by design; play/supernova
  can't reuse eaten matter → needs a play-mode reset decision.
- imgui.ini persists cranked Exposure (17.8) across launches → blown-white views.

## Working rules in force
He suggests, I execute. One verified change → build → deploy-verify → his eyes.
No hype, no one-line pseudo-fixes at architectural problems (this session's lesson).
Self-verify with my own screenshots before handing over. Commit only on his order.
