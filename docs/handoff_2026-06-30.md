# SPACE SYNTH TUBE — Session Handoff (cold-start for a new window)
_Written 2026-06-30 18:15:00 · branch `session-2026-06-30-honest-spacetime-friction` @ `2c6936b`_

READ FIRST: `working_protocol.md` (verify don't assume; ONE verifiable change → confirm → stop;
ground in real data/logs; timestamp STATUS; no hype). Also `src/spacetime/SPACETIME_UNITS.md`
(the live ordered plan + STATUS, sections 1-6) and `docs/bh_root_cause_analysis_2026-06-29.md`.

## Build / run / verify (do NOT use bare `make`)
```bash
bash package_macos.sh                       # builds AND deploys into SpaceSynth.app
stat -f "%Sm %N" SpaceSynth.app/Contents/MacOS/SpaceSynth src/render/particles.metal  # bundle >= source
pkill -f SpaceSynth; open -n SpaceSynth.app  # interactive (Jamal owns the visual verdict)
# headless measurement: ./SpaceSynth.app/Contents/MacOS/SpaceSynth >LOG 2>&1 & sleep N; kill; grep ...
```
Live toggles at runtime = `0x201` (field self-gravity bit0 + adaptive sub-step bit9; external SMBH OFF).

## What shipped this session (verified, kept)
1. **`src/spacetime/spacetime.h`** — honest derived unit system. `c≡1` by construction, `G` derived,
   `kUnitMeters` derived (= 2·r_g of the 594k-M☉ field). Self-verifying `static_assert`s
   (gmSim(field)=0.5, r_s(field)=1.0, 1 sim-time=5.854 s, t_g=2.927 s).
2. **`src/core/units.h`** — re-based onto spacetime.h. The magic `kTimeLapse=20.58` is GONE →
   now derived (`kTLapse·kSimSeconds`). `kTLapse=3.515` is the one explicit warp knob (sim-time
   per wall-second). Behavior-neutral vs the old build (≤0.01%, the kUnitMeters rounding fix).
3. **Accuracy meter** — `accDiag` buffer (particles.metal buffer 14) atomic-maxes the worst
   gravity-kick/light-step ratio → `latestStats.maxAccRatio` → HUD `[accuracy]` line (main.cpp).
   This instrument caught two of my wrong turns. KEEP IT.
4. **Velocity dispersion σ** — `compute_cell_centroids` (spatial_hash.metal) stores σ in the
   previously-unused `cellVelocities.w` (teleport/respawn velocity spikes excluded via VCLAMP).
5. **Chandrasekhar dynamical friction (RANK-1)** — `particles.metal`, injected before the gkick:
   `a_df = −4π·G1²·ρ·lnΛ·m·G(X)·v̂/v²`, X=|v_pec|/(√2σ), `chandraG()` helper (erf via A&S, self-
   regularizing as v→0). `f_relax=4e11` derived time-compression (t_cc≈78 Myr → ~1000 sim-time).
   It cools and conserves mass — correct — but cannot bind the over-diffuse cluster alone.

## The core problem, fully grounded (why there is still no black hole)
Measured across many runs (numbers reproducible via the `[BH-POP]` stderr log + the HUD `[accuracy]`):
- **The cluster spawns 25-60× larger than its own Schwarzschild radius.** Spawn shell is
  `r_inner=25 .. r_outer=60` sim (`particles.cpp`), meanR≈47, but `r_s(field)=1.0` sim. At that
  diffuse scale the gravitational kick is **~1e-6 of a light-step** — far too weak to bind.
  → The cluster **disperses** (meanR 47→78 with friction off, 47→68 on). It does NOT collapse.
- **Mass will not gather.** Bodies stall at the **~50 M☉ seed threshold** (`M_BH_SEED`): once a
  star becomes a "seed" it's skipped as a merge initiator (`merge_stars:2150`) and only grows by
  rare same-cell capture. Measured `gMaxMass` crept 50→53 in 25 s then stalled. Accretion exists
  but is ~1000× too slow. Jamal's words: *"violently thrown into the middle, no way to gather mass."*
- The cold near-radial spawn (`kColdFactor=0.05`) makes infall **slam through the centre** with no
  dwell time to merge; a denser spawn only worsens the through-pass (tested r=3-10, reverted).

### Two hypotheses REFUTED this session (do NOT re-try them):
- "A.2 friction makes it collapse" — WRONG; meanR expands. Friction only *slows* the expansion.
- "Slingshot / saturated-kick heating prevents collapse" — WRONG; `accRatio` stays <0.02 (never
  near the budget of 8) while KE spikes. The KE spikes are teleport/respawn velocity contamination,
  not real heating. Gravity never stresses the integrator at this scale.

## NEXT STEP (the genuine lever): dissipative ACCRETION engine (root-cause RANK-2)
Gravity gathering them is not enough; converging matter must **merge into a growing mass that STAYS**.
Three pieces, do ONE verifiable slice at a time, measure each (watch `gMaxMass`, `encFrac`, `bhStrength`):
1. **Seed sink that keeps eating with a mass-growing cross-section** — remove the stall at
   `M_BH_SEED`; the capture/tidal radius `R_t ∝ R_*·(M_seed/m_*)^(1/3)` grows with mass → runaway
   (first seed out-eats all). Today the seed path (`seed_feed`) is effectively dead/too slow.
2. **Dissipation so infall stays** — inelastic merging already thermalizes relative KE; ensure it
   actually fires at the centre (merge is per-cell, ≤32 samples, contact ≤1.45·cellSize — rate-
   limited). Possibly needs the merge reach / sampling widened near the dense core.
3. **Some angular momentum** so matter spirals in and accretes instead of punching straight through
   (raise `kColdFactor` from 0.05, or virialize) — but only after 1-2, and measure.

THEN the pop fires on its own: the existing density-pop (`renderer.mm:1548`, `[BH-POP]` log) and the
honest geometric horizon (`r_h`, `renderer.mm:1477`, observe-only) will light up once mass concentrates.
Step 2 "accuracy governor" (cap warp when accRatio→budget) is deferred — it has no job until forces
get strong (i.e., until accretion builds a dense core).

## Loose ends / notes
- `[BH-POP]` stderr log restored to original. Accuracy meter HUD is the keeper instrument.
- Dense-spawn experiment reverted to 25-60 with an in-code note (`particles.cpp`).
- `f_relax`, `kColdFactor`, `M_BH_SEED` are the tunable knobs once the accretion path works.
