# HANDOFF 2026-07-19 — science-first session: fake-pull killed, translation clean, ring is the open goal

_Written 2026-07-19 (early AM CEST). Branch `session-2026-06-30-honest-spacetime-friction`.
Read this + `memory/space_synth_bh_accretion_audit_2026-07-18.md` first._

## The one-line state
The instrument now runs smoothly through ALL stages (~50fps). Matter ORBITS instead of
collapsing (the "fake central pull" was a units bug, fixed). The engine's units/translation
are audited clean. **The open goal: a persistent bright ring at the ISCO** — matter that
DWELLS and glows at r≈1–2 instead of plunging into the hole (the "toilet flush") or holding
too far out to see.

## Commits this session (all on the branch, pushed? check `git status`)
- `c4cef52` NEW BASELINE — honest-stack defaults (capture OFF, SPH-visc ON, AMR ON+sweeps4,
  LTRANS ON, all as CODE defaults so a bare Finder launch works, no env needed) + **the dt
  fix** + honest mass count.
- `83756a6` Unify play/rest physics — one ±64 domain (killed the ±3 tube-scale switch = the
  "gridy squares + Dyson mergers after play"). AMR fine box carries near-core resolution.
- `0edde58` Render — rotation ease-in (spin ramps, no snap at hole-formation) + center the
  render spin/shadow/membrane on the honest hole `bhPos` (matters after PLAY — hole forms
  off-centre).

Uncommitted: `imgui.ini` (his local exposure state — NEVER commit), `src/core/particles.cpp`
(R_DISK comment-only, value still 18 = baseline). App binary rebuilds via package_macos.sh.

## ⭐ THE science-first win (the template for everything next)
**The "fake central pull"/toilet-flush at t=0 was a spawn↔integrator UNITS bug**, not physics:
the Verlet stores velocity as per-step displacement `v·dt`; the integrator pins `dt=0.0165`
with `tcv=dt/dtPrev=1` (renderer.mm:1002-04) so there's NO frame-1 correction, but the spawn
baked `v` with `kDt=1/120=0.0083` (particles.cpp:220) → every orbit spawned at **0.505×
v_circ** → centrifugal support only 0.25 of gravity → a spurious 0.75·g inward pull. FIX:
`kDt = 0.0165`. Measured: core-plunge HALVED (BAND r<0.4 410→211), field HOLDS a thin disk
(SHAPE σ 5→22). **RULE: a circular orbit has pull:orbit = 1:1, zero net inward. Any net pull
in the sim = v<v_circ = a units bug. Compute the real ratio from units, find where the sim
disagrees.** This cracked "two weeks of problems in 20 minutes" (his words).

## Translation audit — DONE, result: CLEAN
All 5 quantities consistent across spawn↔integrator AND compute↔render: time (kDt=dt),
mass (spawn+gravity both use `imf::totalMassMsun`=Σ`massOfId`, UNSCALED; the 1.89e5-vs-5.94e5
gap is only the Size `massScale` on the DISPLAY, cancels in `G1=gravGM/massTotal=gmSim(1)` —
the "BH pull" suspicion was a FALSE ALARM, verified via imf.h), length (plateRadius uniform),
velocity (`speedCap·dt`), spin-rate (Ω·poseTime=rad), temperature (derived `u·c²/k_B`). No
hidden mistranslation. Remaining minor (render posed-spin only): #1 center-on-hole DONE;
#2 posed spin uses M(<r_h) for all radii → outer disk under-rotates (should use M(<r) profile);
#3 spin on wall-time vs physics fixed-dt (FPS-dependent spin speed); #4 (cosmetic) Size→gravity
slider no-op.

## THE OPEN TASK (next change, mid-flight when handed off)
**Spawn the disk as a visible ANNULUS just OUTSIDE the ISCO.** Why: the vanishing is NOT a
drain we can tune away — it's WHERE matter is born.
- Matter born INSIDE the ISCO (r≲1.2) has NO stable orbit → it can only plunge → the visible
  "massive toilet" (the nucleus spawns r<3, concentrated r<1 = inside the ISCO).
- Matter that HOLDS is spawned too far out — the disk settles at meanR≈22–33, off the default
  camera (±4.8 sim; ±24 max zoom-out). So you never see the disk that works.
- FIX: give the disk an INNER CUTOFF at ~1.5 (just outside ISCO≈1.2) so nothing is born inside
  the ISCO, and an outer edge ~5 so it's IN VIEW. Then all disk matter holds a fast (~0.4c at
  r≈1.5) bright orbit at the shadow's edge = the ring, by construction.
- EXACT SPOT: particles.cpp:148 `float rr = R_DISK * std::pow(u01(rng), 1.0f/1.7f);` — change
  to draw rr in an annulus `[R_IN≈1.5, R_DISK≈5]` (e.g. area-weighted, avoid the r^-0.3 inward
  pile that dumps matter inside the ISCO). Then rebuild, PROBE the BAND (does r=1–2 fill AND
  HOLD over minutes, not just transit inward?), and HIS EYES (bright ring, in view, doesn't
  toilet). Note the disk EXPANDS past R_DISK (dt-fix true-circular + ecc), so R_DISK=5 may
  settle ~r8 — tune to keep it in the ±4.8 view.

## FAILURE LEDGER (don't repeat)
- R_DISK 18→8 (pre-dt-fix) and 18→3 (post): BOTH imploded — matter piled INSIDE the ISCO and
  plunged (886–986 in r<0.4). His eyes: "implode then explode, no ring." The early BAND
  "409 in r=0.4–2" was matter TRANSITING inward, NOT a stable ring — I mis-read a transient;
  HIS EYES are ground truth, not a BAND snapshot.
- dynfric is NOT the drain — it's the COOLING (damps `vpec = v − local_mean_flow`, orbit-
  preserving) that lets the disk go thin/cold enough to COLLIDE and MERGE (his rings). Turning
  it off (SS_PLAY_SKIP=dynfric) = NO collisions/mergers/rings. Keep it.
- LTRANS (bit25) is the α-disc L-transport (the intended accretion drain) but SS_NO_LTRANS
  barely changed the flush — the outer disk held either way; the flush is the inner-born matter.
- Lensing bit8 ON = smears particles into distorted "eye" arcs (it DISPLACES particle screen
  positions instead of adding a lensed SECOND image). Needs a primary+secondary rework, NOT a
  toggle. Kept OFF. This is queued AFTER the ring.

## RITUALS (enforced)
- Build+deploy: `bash package_macos.sh` (NEVER bare make). Verify bundle binary/metallib
  timestamps ≥ source before testing. clangd "file not found"/ImGui errors in the editor are
  NOISE (missing include paths) — the real build says "Built target SpaceSynth".
- Probe headless: run the binary directly, capture stdout+stderr; grep `[BAND]` (radial
  histogram /1000), `[SHELLV]` (vr vs vt = plunge vs orbit), `[SHAPE]` (σ per axis),
  `[HORIZON]` (r_h, M(<r_h)), `[GRAV]` (com, meanR, bh=, Mmax). LET IT RUN MINUTES — the
  collapse/settle takes minutes; never verdict off a 25–50s transient.
- I change, HE plays. Build+open, then STOP for his verdict. Never drive synthetic MIDI.
- His eyes are ground truth over any probe number.
- Commit ONLY on his explicit order.
- `pkill -x SpaceSynth` before each launch; `open -n SpaceSynth.app` for a clean Finder-style
  launch (now runs the honest stack by default).

## AFTER the ring
Proper lensing (primary+secondary so the disk wraps the shadow without smearing) → posed-spin
#2/#3 → the merger-ring aesthetic he loved ("super close", two mergers became rings) is the
target look.
