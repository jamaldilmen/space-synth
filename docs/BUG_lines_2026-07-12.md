# 🔴 OPEN BUG — straight LINES / GAPS in the rest cluster
**Written 2026-07-12 12:12:58. Show is TOMORROW. This is the only blocker.**

---

## ⚡ UPDATE 2026-07-12 13:20:48 — ROOT-CAUSE SESSION (data-driven, big findings)

**Status: MITIGATED for the show (Jamal: "good enough for now"), root cause NARROWED
but the first-seconds carver is NOT yet named. Launch the show via `run_show.sh`.**

New instruments (all in the working tree):
- `SS_INERT=1` (+`SS_INERT_KEEP=tok,...`) — all-forces-off isolation ladder (main.cpp ~239)
- `SS_DUMP=/path.bin` (+`SS_DUMP_TICK=n`, probe ≈10 s each) — full-field x,y,z dump (main.cpp, probe block)
- `SS_PLAY_SKIP=dynfric` — bit24 gates the previously UNGATED Chandrasekhar block (particles.metal ~1277)
- `tools/lanes.py` + `tools/analyze_dump.py` — objective lane detector on a dump. **No more
  bisecting by eyes alone: `lanes.py` sees exactly what Jamal sees.**

PROVEN, in order (each by dump analysis, corroborated by Jamal's screen):
1. **The lanes are IN THE PARTICLE DATA** — not the render. (Dump of an inert run shows them.)
2. **The t=0 spawn is CLEAN.** Standalone-compiled `particles.cpp` spawn (same RNG seed 42) has
   no lanes. Plummer sampler exonerated.
3. **The lanes are carved in the FIRST ~seconds and then merely preserved.** At the first probe
   (~10 s) the main lane is already full depth (0.52); at 30 s it is unchanged (0.50). Same
   fixed location every run/config (deterministic seed): main lane = the whole z∈[−6,−5] cell
   layer (one cellSize wide), plus dips exactly at cell boundaries z=−8, −2, +2, +8 and
   structure at z≈+3..+4. z-axis only at ≥6σ.
4. **Carver #1 (deepest): the UNGATED Chandrasekhar dynamical-friction block**
   (particles.metal ~1277) — no bhToggles bit, alive even under SS_INERT, drags every star
   toward a per-cell mean velocity built by compute_cell_centroids from only ≤32 of an
   uncapped cell population, coef capped at 0.5/frame (fRelax=4e11). Inert+centroid-only run:
   lane depth 0.18 (this block alone carves hard). It is also why the 07-10/11 sessions kept
   converging on "SPH viscosity": both are per-cell velocity-dispersion killers; SPH visc was
   the AMPLITUDE knob, dynfric was the constant.
5. **Exonerated as the residual's source (lane persists ~0.5 with each off): SPH kernels,
   merge_stars, dynfric itself.** ⚠️ The morning "SPH ruled out" stands, but note it ran WITH
   dynfric live, so it could never have found dynfric either.
6. **Carver #0 (OPEN): something in the first seconds** carves the scar with dynfric+SPH+merge
   all off. Still-live per-cell suspects in those frames: bit0 near-field self-gravity (reads
   the same ≤32-sample cellCentroids), PM/SOR (bit10), seed capture (bit2), first-frames stale
   hash (`hashFresh`), scatter's 32-per-cell keep. NEXT SESSION: dump at tick with
   SS_INERT_KEEP ladder = pm alone → fieldgrav alone → capture alone, at SS_DUMP_TICK=1;
   or instrument the first 120 frames directly.

Also real (kept): per-frame sphForce clear in renderer.mm; centroid-pass skip leaves
cellVelocities zeroed → dynfric self-disables (that was the mechanism of the "clean" all-skip run).

**A toggle is not a fix:** `run_show.sh`'s flags are a MASK for the show. The honest fixes are
(a) give dynfric a real gate + sane coefficient derived from a full-population σ (not 32-sample),
(b) name and fix carver #0, (c) then re-enable the physics one term at a time with lanes.py as
the regression test.

---
Repo: `/Users/airy/SPACE SYNTH/SPACE-SYNTH-TUBE`
Branch `session-2026-06-30-honest-spacetime-friction` @ `d6f5217` + uncommitted (see bottom).

---

## THE SYMPTOM (Jamal's words, his screen is ground truth)
> "the fucking lines in there bro. like gezogen mit nem Lineal."

In the star map **at rest** (no keys played), the cluster is cut by **ruler-straight streaks and dark
lanes/gaps**. They are grid-axis-aligned. They **worsen over time** as the core densifies. fps also
decays 60 → 30/20 over minutes and recovers after playing.

**"Slices"/"slabs" (thick) and "lines" (thin) are the SAME artifact at different amplitudes.**
Do not treat them as two bugs. That mistake cost a full day.

---

## ⛔ WHAT IS RULED OUT — with evidence. DO NOT RE-DIAGNOSE THESE.

**1. SPH is NOT the cause. Ruled out 2026-07-12 12:11 (definitive).**
Ran the real bundle with `SS_SPH_SKIP=density,pressure,force` (verified present in the process
environment via `ps eww`, not just assumed). All three SPH passes skipped → **lines still there,
unchanged.** Screenshot confirmed by Jamal.
⚠️ This **invalidates the entire 2026-07-10/11 diagnosis**, which blamed SPH viscosity and shipped
`uiTogSphVisc=false` in commit `61d3d40` ("kill the star-map slices"). **That commit message is
wrong.** The old bisection that "proved" SPH was the culprit is not reproducible now and should not
be trusted. Whatever it showed, SPH-off today still lines.

**2. The stale SPH force buffer — REAL BUG, FIXED, but NOT the cause of the lines.**
`compute_physics` does `gacc += sphForce[id].xyz` for EVERY particle (`particles.metal:1341`), but
`sph_force` only WRITES the ≤32 particles per cell that `scatter_particles` keeps. `sphForceBuffer`
was memset only on allocation (`renderer.mm:733`) and never per frame → an unwritten id kept its LAST
force **forever**, re-applied every frame = frozen constant acceleration → straight-line ejecta at
the speed cap, with no internal-energy debit booked (explains the ledger's `W=1.9e7, dyn=0`).
**Fixed** (uncommitted): added a per-frame `fillBuffer:sphForceBuffer value:0` to the existing
clearBlit in `renderer.mm` (~line 1394). **Keep this — it is unambiguously correct — but it did NOT
remove the lines.**

**3. Two other fixes tried 2026-07-11 night, BOTH FAILED, BOTH REVERTED — do not repeat:**
- Un-gating the `PdV` energy debit from the viscosity branch (`spatial_hash.metal` ~782). Correct
  physics, no visual change, `dyn` stayed 0.
- Replacing the `1e-12` ρ floor with a physical self-contribution floor `ρ ≥ m·W(0,h)`. Poison
  (non-finite counter) EXPLODED 250k → 40M. (Careful: `h` is not in scope at line 644, use
  `u.cellSize` — a first attempt silently failed to COMPILE and a stale binary was tested.)

**4. Earlier: gravity/SOR were "ruled out" on 2026-07-11 — TREAT AS UNRELIABLE.** That ladder used
`SS_NO_PM_GRAVITY`, which was an INVALID test (bit10 overrides bit0, so gravity was never actually
off). It was then re-tested with `SS_NO_GRAVITY`, but given the SPH conclusion from that same session
has now collapsed, **re-verify gravity from scratch rather than trusting it.**

---

## 🎯 WHERE TO LOOK NEXT (nothing here has been tested today)
The lines are produced by something OTHER than SPH. Candidates, in the order I'd bisect:

- **The per-cell merge / collision path.** Prior sessions found this rasterizes at CELL granularity
  and produced the "supernova CUBES" — noted in the memory as *"the SAME grid enemy as the SPH
  slabs"*. `merge_stars` + seed capture (bit2, `0x4`) and seed-seed merge (bit3, `0x8`) use a
  `1.4·cellSize` reach — grid-scale, axis-aligned. **This is my top suspect now.**
  Test: `SS_SPH_SKIP=merge` (already supported in `renderer.mm:1229`).
- **Gravity**: PM/Poisson-SOR (bit10 = `0x400`), field self-gravity (bit0 = `0x1`), AMR fine grid
  (bit15 = `0x8000`, default OFF). Softening ε = cellSize = grid scale → axis-aligned force error.
- **Legacy grid "pressure"** (bit14 = `0x4000`, `uiTogNoLegacyPressure = true` retires it). Memory
  calls this "THE substrate noise pump". It is currently RETIRED by default, but confirm bit14 is
  actually set at runtime.
- **`dynamic_cap` / cohesion / centroid** — per-cell (O(N) grid) forces, axis-aligned by construction.
- **Render** is unlikely (a frozen `SS_START_PAUSED` spawn was gorgeous) but not re-verified today.

## 🔧 THE FIRST THING TO DO
**The isolation ladder was STRIPPED from `main.cpp`.** `SS_INERT` / `SS_ONLY_SPH` / `SS_NO_GRAVITY` /
`SS_START_PAUSED` no longer exist (only `SS_SPH_TEST`, `SS_PLAY_SKIP`, `SS_NO_LEGACY_PRESSURE`
remain; `SS_SPH_SKIP` still works in `renderer.mm`).
**Re-add an `SS_INERT` (all forces off) launch flag first**, confirm the field is clean with
everything off, then switch forces back on ONE at a time until the lines appear. That single test
names the culprit. Do not theorize before running it.

## ⚠️ HOW TO NOT WASTE THE DAY
- **`bash package_macos.sh`** — NEVER bare `make` (it does not update the .app bundle). Then verify
  `SpaceSynth.app/Contents/{MacOS/SpaceSynth,Resources/default.metallib}` timestamps are NEWER than
  the sources. "Change did nothing" → **suspect a stale binary FIRST.**
- `open` does NOT pass env vars reliably. Run `SS_X=1 ./SpaceSynth.app/Contents/MacOS/SpaceSynth`
  directly and confirm with `ps eww <pid>`.
- **Jamal's eyes are ground truth.** Never ask him to re-verify or claim you can't reproduce.
- ONE change → build → verify it landed → let HIM look → STOP. Never batch.
- **A toggle is not a fix.** Turning a force off until the artifact looks quieter is a MASK — that is
  exactly how `61d3d40` happened. Name the mechanism or say "not solved yet".

## 🌳 UNCOMMITTED WORK (do not lose)
- `renderer.mm` — per-frame sphForce clear (**the only change made today; keep**); AMR slice-2 wiring
  (bit15, fine buffers, prolongate + fine solve, `SS_AMR_SWEEPS`).
- `spatial_hash.metal` — `poisson_sor_fine`, `prolongate_coarse_to_fine`, and the **SPH sub-step perf
  fix** (N keyed on sound speed only → 15fps → 60fps; independently good).
- `particles.metal` — AMR fine force (bit15); **the tube fix** (spherical cap at rest, cylindrical
  only while playing).
Nothing is committed because nothing is stable. That is deliberate.
