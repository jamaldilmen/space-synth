---
name: space-synth-handoff-2026-07-12
description: "SPACE SYNTH TUBE — COLD-START handoff 2026-07-12 03:30 (CORRECTED 03:52). 🔴 #1 OPEN BUG: SLICES AND LINES ARE THE SAME BUG — the broken SPH force (P/ρ² singularity from the 32-per-cell density cap). NOT solved; visc-off was a MASK. Nothing committed since d6f5217. Also this session: full-codebase read, AMR nested-mesh gravity built, tube killed."
metadata: 
  node_type: memory
  type: project
  originSessionId: 859d7120-5215-4158-a1d2-dfdfa5929bd5
---

# COLD-START HANDOFF — 2026-07-12 03:30:00  (CORRECTED 2026-07-12 03:52:11)
Branch `session-2026-06-30-honest-spacetime-friction` @ **`d6f5217`** (+ uncommitted, see WORKING TREE).
Read [[working_protocol]] + [[feedback_believe_his_eyes]] FIRST. I broke the believe-his-eyes rule
TWICE today (doubted the slices, doubted the fps decay with a too-short log). **His screen is ground
truth. Do not "fail to reproduce" — find the mechanism.**

> **CORRECTION — the first draft of this handoff claimed "SLICES SOLVED". That was FALSE.**
> Jamal caught it: **the slices and the lines are the SAME BUG.** See below. Nothing is solved.
> That is precisely why nothing is committed. Do not let the earlier optimism back into this file.

---

## 🔴🔴 #1 OPEN BUG — SLICES *AND* LINES: ONE BUG, THE BROKEN SPH FORCE (start here)
Jamal, all day, escalating: "the fucking lines in there bro. like gezogen mit nem Lineal."
Ruler-straight streaks + gaps inside the rest cluster. Worsens over time. **This blocks all BH work.**

### THEY ARE THE SAME BUG — viscosity was only the AMPLITUDE KNOB
One mechanism: the SPH force is numerically broken (P/ρ² singular, below). Viscosity only set how
hard it kicks:
- **viscosity ON** → violent kicks → **thick slabs / slices**
- **viscosity OFF** → weaker kicks → **thin ruler-straight lines**

So `uiTogSphVisc=false` (committed in `61d3d40`, whose message wrongly says *"kill the star-map
slices"*) **MASKED the symptom, it did not fix anything.** The bisection that "found viscosity" only
found the amplitude knob, not the cause. **Treat `61d3d40`'s claim as retracted.** The real fix must
make the SPH force well-posed — then viscosity can come back on (it's physically required).

### What it IS (measured, high confidence)
The **SPH pressure force is an ENERGY PUMP**, blasting particles to the speed cap; they fly dead
straight → the lines. Evidence from the code's OWN ledger:
- `[CLOSURE] W=1.3e7 … 1.9e7   dyn=0   clamp=0   poison=250k` — W = work done by the SPH force.
  It is ENORMOUS and the conjugate internal-energy term (`dyn`) is ZERO. **The books do not close.**
- `speed avg 0.05–0.13  max 1.000` — **max speed PERMANENTLY PEGGED AT THE CAP.** Something is
  continuously accelerating particles to c. Straight-line ejecta = the lines.
- `poison` = the shader's own non-finite (NaN/Inf) counter on `dudt`. Hundreds of thousands.

### The mechanism (grounded in the code)
`spatial_hash.metal` `sph_force`:
```
rhoI = max(densityIn[originId], 1e-12f);         // ← a 1e-12 floor IS a 1e12 SINGULARITY
PiOverRhoI2 = PiCur/(rhoI*rhoI) = (γ−1)·u/rhoI;  // → explodes when ρ→0
```
**Why ρ is 0 for many particles:** `sph_density` computes ρ only for the **first 32 particles per
cell** (`homeCount = min(cellCounts,32)`, tile of 32 threads). In a dense cell of thousands, everyone
past #32 **never gets a density written** → reads ~0 → `1e-12` floor → singular P/ρ² → catastrophic
kick. **It gets worse as the core densifies** — which is exactly why the lines worsen over time and
why fps decays (the flung particles pile up in a few cells).

### ⚠️ TWO FIXES ATTEMPTED TONIGHT — BOTH FAILED, BOTH REVERTED. Don't repeat them:
1. **Un-gate PdV from viscosity.** The `PdV` energy debit is wrongly fused inside `if (viscOn)`
   (line ~782), so when we set `uiTogSphVisc=false` (this morning's slab fix) the pressure force kept
   pushing but stopped paying → unbalanced work. Splitting PdV (with bit11) from ½Π (bit12) is
   **correct physics** BUT did not fix the lines, and `dyn` stayed 0 (the ledger's dyn may itself be
   visc-gated — CHECK THAT). Reverted.
2. **Physical density floor** `ρ ≥ m·W(0,h)` (a particle's own self-contribution) instead of 1e-12.
   Physically right, but **poison EXPLODED 250k → 40 MILLION**. Something else is wrong. Reverted.
   (First attempt also failed to COMPILE — `h` not in scope at line 644; use `u.cellSize`. I shipped
   a stale binary and wasted his time. VERIFY THE METALLIB TIMESTAMP EVERY BUILD.)

### 🎯 THE REAL FIX (do this — it is the ONLY thing that ends both symptoms)
**Give EVERY particle a density. The 32-per-cell cap IS the disease.**
`sph_density` writes ρ for only the first 32 particles in a cell (`homeCount = min(cellCounts,32)`,
32-thread tile). Everyone past #32 in a dense cell reads ρ≈0 → the `1e-12` floor → singular P/ρ² →
catastrophic kick. Loop the tile over the cell in chunks of 32 (or raise/remove the cap) so ρ is
computed for **all** members. Nothing downstream is trustworthy until this holds. Then:
(b) un-gate PdV from viscosity, (c) re-enable viscosity behind a Balsara / cold-subsonic gate.

### ⚠️ The bit11-off idea is a MASK, not a fix — label it as one
Turning SPH pressure off at rest (`uiTogSphPressure=false`, bit11) WILL make the lines disappear
(isolation proved it: **INERT = gorgeous, zero lines**), and at rest `u` sits at the cold floor so
pressure *should* be ≈0 anyway. But that is **the exact same move as turning viscosity off** — it
hides a broken force instead of fixing it, and the reaction engine NEEDS that force. If it is used at
all, use it as a **temporary unblock for BH work, explicitly flagged**, never as "solved". Do not
commit it as a fix. **Do not repeat 61d3d40.**

---

## ✅ WHAT WE ACCOMPLISHED TODAY (real, verified on his screen)
1. ~~SLICES SOLVED~~ — **RETRACTED. NOT SOLVED.** The bisection (SS_INERT / SS_ONLY_SPH /
   SS_ONLY_SPH_P) did narrow it to the **SPH force**, and it did kill my first two theories (NOT
   gravity, NOT SOR — my "PM-off" test was invalid because bit10 overrides bit0). That much stands.
   But `uiTogSphVisc=false` (committed `61d3d40`) only turned the **amplitude knob** down: slabs
   became lines. **Same bug, quieter.** The commit message on `61d3d40` is wrong; the real state of
   this bug is #1 OPEN, above.
2. **PLUMMER SPAWN committed** (`61d3d40`) — proven clean (frozen + inert both gorgeous).
3. **FULL-CODEBASE READ** (he demanded it, rightly) → [[space_synth_full_codebase_model]].
   Found the ONE architectural wall, corroborated by the old 4-agent audit:
   gravity is a **collisionless softened mean-field with ε = cellSize = 1.0 sim = the horizon scale**
   → it *cannot* collapse, and r_s is SUB-CELL. Slabs/cubes/planes were all facets of this one grid.
4. **AMR (nested-mesh gravity) BUILT** — his call: "how NASA would do it". Committed `d6f5217`
   (slices 0+1) + uncommitted slice 2. Key simplification: comShift already pins the core to the
   origin → we need ONE FIXED fine patch, not a roaming octree.
   - Slice 0: **measured the wall** — gravity concentrates only ~3k M☉ within 0.5 sim (horizon needs
     **297k**) and mass stalls at the ~1-sim softening floor. r_h = 0 forever.
   - Slice 1: fine 128³ grid over ±2 sim (**cellSize 0.031 sim, 32× finer**), `bin_fine_mass`,
     `poisson_sor_fine` with **nested coarse-Φ boundary**, `prolongate_coarse_to_fine`
     (2-level multigrid seed). Fine well now resolves DEEPER than coarse.
   - Slice 2: fine force wired (bit15) — particles inside the patch feel −∇Φ_fine.
   - **AMR is now essentially FREE** (60 fps) after cutting fine sweeps 80→16 (prolongation made 80
     obsolete; 80 was costing 10 fps).
5. **THE TUBE KILLED.** He was right all along. The `dynamic_cap` clamped **rXY only (x,y), z FREE**
   = a CYLINDER — and it ran AT REST. Star map was trapped in a tube with a flat wall (stars piled on
   it; the clamp also kills outward velocity → they STAY). Fixed: **spherical cap at rest, cylindrical
   tube only while PLAYING** (which is what he wants). Verified: proper round cluster zoomed out.
6. **SPH sub-step perf bug fixed** — `NSub` was keyed on `(c_s + |v|)`, but its own comment says the
   sub-step exists for the *thermal* u↔v stiffness. Cold-but-fast cores burned **8×** the 27-cell
   loop for a ~zero force. Keyed on `c_s` alone → **compute went from 21→88 ms (15 fps) to a FLAT
   ~19 ms (60 fps)**, density-independent. Big standalone win, benefits the shipped app.
7. **The merge runaway WORKS** — with AMR + merge (dissipation), a dominant body grew
   **50 → 796 M☉**, seeds 0→13, and a visibly dense bright core formed. First time. The BH path is
   alive; it's the lines/energy-pump that's blocking it.
8. Built him a **visual build timeline artifact** (Feb 23 → Jul 11, 344 commits, ~936k chars).

---

## 🌳 WORKING TREE (uncommitted — DO NOT LOSE)
`src/render/spatial_hash.metal` — `poisson_sor_fine` (nested coarse-Φ BC), `prolongate_coarse_to_fine`,
  **the sub-step fix (c_s only)** ← keep, it's the 60fps win.
`src/render/renderer.mm` — AMR slice-2 wiring: bit15, fine buffers bound to compute_physics (19/20),
  prolongate + fine-solve dispatch, fine sweeps 16 (`SS_AMR_SWEEPS`), `[CORE]`/`[AMR]` logs.
`src/render/particles.metal` — AMR fine force (bit15) in compute_physics; **the tube fix** (spherical
  cap at rest / cylinder only on play).
He said: **don't commit until the lines are fixed** ("nothing to commit yet, it's not stable").
The sub-step fix + tube fix are independently good and worth committing once lines are gone.

---

## 📋 THE FULL TODO (ordered)
1. 🔴 **FIX THE SPH FORCE — this is the slices AND the lines** (above). Real fix = every particle
   gets a density (kill the 32-per-cell cap). Then un-gate PdV, then viscosity back on behind a
   Balsara/cold-subsonic gate. **No more amplitude knobs. No more masks.**
2. 🔴 **fps decay** (60 → 30/20 over minutes, recovers after play). Almost certainly the same
   pile-up from the flung/pumped particles. Re-check after #1 — may just vanish.
3. **AMR slice 3** — with the core actually crushing, does `r_h` finally lift off 0? Render the
   particles inside r_h AS the horizon (BH = particles, canon). Then slice 4: geometric capture,
   retire the `M_BH_SEED=50` mass cutoff + the grid-scale seed path (bit2/bit3 use `1.4·cellSize`
   reach — THAT is what made the CUBES; `merge_stars` itself is spatially exact and fine).
4. **The wandering seed** — a dominant body formed then drifted off into deep space. Chandrasekhar
   dynamical friction (∝ mass) is supposed to SINK it. Either it's too weak or the merge hands it a
   momentum kick. (bit4 origin-pin exists but is a hard leash — he does NOT want the BH pinned.)
5. **Re-enable SPH viscosity** behind a Balsara / cold-subsonic gate (no shock heating right now).
6. **Over-exposure** — additive blend + bloom re-blows the tonemapped core to white (postfx).
7. **Mass→size is `M^0.4`** (sqrt-compressed) → a 100× mass gain shows as only ~6× size; growth is
   nearly invisible. Unify to `M^0.8`.
8. **The `[CORE]` readback race** — reads `.contents` before the GPU finishes → occasional zeros.
9. Housekeeping: temp `SS_*` env scaffolding, `seed_feed` is dead code, `ui/mod_menu.*` unused.

## 🔧 KNOWN-GOOD REFERENCE STATES (for bisecting)
- `SS_INERT` (all forces off) = **gorgeous, zero lines** — the clean baseline.
- `SS_SPH_SKIP=force` = compute FLAT ~12 ms, 60–80 fps, no pump.
- `SS_AMR=1` = nested-mesh gravity on (default OFF).
- `SS_SPH_SKIP=merge` = no dissipation → cluster just virializes, never collapses.
- Build: `bash package_macos.sh` — **ALWAYS verify the metallib timestamp changed** before testing.
