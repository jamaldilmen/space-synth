---
name: space-synth-agenda-2026-07-08
description: "THE reorganized master agenda after the 2/10 postmortem — Checkpoint A (starmap right, unified thresholds) blocks everything; then physics, perf, parked, hygiene"
metadata: 
  node_type: memory
  type: project
  originSessionId: 55250866-86d7-46cb-b259-243f194b30a0
---

**Last Updated: 2026-07-08 19:25:00**

## 🧊 COLD-START HANDOFF (new window, 2026-07-08 19:30:00)
- APPROVED build 19:21:02 deployed in `SpaceSynth.app` — Jamal: "perfect".
- ⚠️ ENTIRE DAY UNCOMMITTED across 7 files (incl. PARALLEL-session WIP: seed
  bit2 defaults in app_state.h, sphU buffer(18) plumb in particles.metal).
  Single-checkpoint commit PROPOSED, his go NOT yet given ("cool" ≠ commit).
  Do NOT checkout/reset particles.metal — it mixes my work + theirs.
- Build/deploy: `bash package_macos.sh` (NEVER bare make), verify bundle
  stat timestamps ≥ source, `pkill -9 -f SpaceSynth; open -n SpaceSynth.app`.
- Measure, don't guess: [SHAPE]/[VEL] probes print per-240f (mean+σ+drift of
  1000-star readback). Play-state repro WITHOUT his eyes: play
  scratchpad tone via `afplay` → mic → voices>0 → read drift in log.
  Skip-envs: SS_PLAY_SKIP=impulse,swirl,web · SS_RENDER_SKIP=points,bloom,
  post,syphon,ui · SS_RENDER_TINYPTS/MINVS · SS_SPH_SKIP (older).
- His working contract after the 2/10 postmortem: name the canon before
  law-changes, announce route changes, revert-to-approved first, calibrate
  defaults not faders, ONE change → his verdict → stop.


## ✅ APPROVED STATE ("perfect", Jamal 2026-07-08 19:24) — build 19:21:02
Play = pre-3D-wave code EXACTLY (angular Chladni sculpt + radial breathing +
XY tube cap 3 / rest cap 100 + original jitter). The whole tube/true-3D-wave
slice (spherical + cylindrical eigenmodes, Gor'kov, first-order wave motion,
Boltzmann fog, z-caps, STAR_MAP_CAP 62) REVERTED surgically — parked until the
play-force stack gets a measured cleanup. KEPT: blue-noise spawn (calm launch),
MERGE_RSUN 0.01, impulse ACOUSTIC-LIMIT CLAMP (min(impulseForce,1) — convicted
by the SS_PLAY_SKIP drift hunt: baseline mean drifted to (0,-10,-8) under a
mic tone, impulse-off stayed centered; the crescent bug), Checkpoint A render
(size law + unified colour + exposure + bloom-through-bleach), TEMP probes
([VEL]/[SHAPE], SS_PLAY_SKIP gates for impulse/swirl/web).
LESSON BANKED: the crescent survived FOUR wave-law "fixes" because the culprit
was the legacy onset shockwave (deltaAmp·400·densityScale(4) ≈ 1600·deltaAmp
kicks) — measure force channels FIRST next time (the drift-hunt method works:
afplay tone → mic → voices → [SHAPE]/[VEL] readout, no eyes needed).
NEXT: commit-split of the day (awaiting his go), then agenda B/C.
⚠️ 3D-wave findings preserved for the retry: nodal sets are measure-zero (need
finite-T Boltzmann term), Gor'kov −Ψ∇Ψ is the symmetric sand law, cavity
eigenmodes quantize k, parcel scale is ~0.05 sim/frame — all correct pieces,
blocked only by the unrationalized play-force stack.

## LIVE SLICE — TRUE 3D WAVE IN THE TUBE (pass 2, awaiting verdict)
Pass 1 spherical Ψ=j(kr)·Y REJECTED ("ball killed the godlike length; still a
2D surface on the sphere"). Pass 2 LIVE (build 14:10:56): cylindrical-cavity
modes Ψ = J_m(k_ρρ)·cos(mθ)·cos(k_z z), dispersion k_ρ²+k_z²=(2πf/c)² exact,
c=880 sim/s (A4→λ/2=1 sim). Tube cap restored verbatim. Fake radial breathing
deleted; webbing/ridge "phi" slot now = axial z. Blue-noise spawn shipped
(dart-throw, 10M baked, min gap 0.26, ~3s bake at launch — his earlier
"stuck, crashed" report NOT reproduced, unresolved). Capsule-at-rest mystery:
physics = perfect ball ([SHAPE] σ 27.7/28.0/27.7); streak-law test (Streak
Len→0) still owed by his eyes. MERGE_RSUN 0.01 verified calm (biggest 55 M☉
in 75s). Checkpoint A verdict partial: merged stars "somewhat right"; yellow
tones + capsule remained → this slice.
Mirror: `docs/agenda_2026-07-08.md` in the repo. Supersedes the D-item ordering
of [[space_synth_tube_agenda_2026-06-14]] for current work; canon references:
[[space_synth_powder_toy_lessons_2026-07-04]] (thresholds NOT phases),
[[space_synth_governing_model]] (3 scalars, one law). Postmortem contract:
[[feedback-starmap-2of10-postmortem]].

## A. STARMAP RIGHT — blocking, Jamal must sign off the checkpoint build
1. Size law: revert to approved R∝M^0.8 + tanh ceiling (bbbe6c8 "looks amazing").
2. Unified temperature→colour: ONE law `blackbody(Teff(M) + heat·PlasmaHeat +
   ke·ColourSpectrum)` — sliders live on the star map (were play-only). Full
   envelopePhase/starMix teardown (thresholds only, Powder-Toy) is the follow-up
   slice A2b — checkpoint A seeds the unified law without dismantling play.
3. Exposure DEFAULT calibrated: bulk field visible at first glance (space feel),
   giants bleach white, core resolves. Fader stays for exploration.
4. Bloom scene-referred: glow composited BEFORE tonemap+bleach (was after —
   repainted un-bleached yellow, the reason the blob stayed yellow).
5. Launch calm: min-sep lattice spawn (SHIPPED, verified live in spawn path) +
   MERGE_RSUN_SIM 0.397→0.01 rate calibration (in tree, needs launch-log verify:
   biggest body must stay ≈ IMF max ~10 M☉ for minutes, novas rare).
6. Stars must not pop in/vanish: respawn/recycle audit (open).

## B. PHYSICS — the BH foundation (after A)
- Reaction ladder rungs 3+ (disrupt/plasma by v_rel/v_esc; rungs 1–2 landed via
  parallel session 5d27abb/0a4f283). NOTE parallel session also: seed feeding
  bit2 ON (feed=0 root cause found), momentum-conserving feeding ledger.
- Verify feed>0 + BH growth regime; residual pump tail (per-sub-step dudt);
  flat-explosion-on-release bug (tubePhase hash extent 3↔64 suspect);
  emergent BH pop by geometric threshold (same unified-threshold doctrine).

## C. PERFORMANCE — 100fps gate @2M (currently ~45fps headless, 20.5ms)
- Biggest lever: compute/render OVERLAP via snapshot buffer — render is
  primitive-bound (bare 2M points = 3.5ms floor, VS free, fill ~1ms; measured
  via SS_RENDER_SKIP + kTinyPoints/kMinimalVS probes) → overlap hides all ~6.6ms
  → ~70fps. Then compute diet / rest-play split. Bloom 1.2ms, postfx 0.7ms.
- Syphon hasClients gate shipped (needs clean re-verify — first attempt
  contaminated by his live instance). NEVER downscale resolution.

## D. PARKED (his explicit calls)
- Legacy-toggle god-forms / true-collision feel: re-enable later, owes tiling.
  ⚠️ CONFLICT: parallel session set uiCollisions/uiBondNetwork=true
  "engine-permanent" in app_state.h; he told THIS session OFF for fps. Needs his
  one-word ruling.
- Oscilloscope/Lissajous; HUD polish; audio LAST.

## E. HYGIENE
- Tree holds the whole 2026-07-08 night uncommitted (exposure fader, bleach,
  true-flux, lattice spawn, Syphon gate, SS_RENDER_SKIP probes, MERGE_RSUN
  0.01). After his A-verdict: split keep/revert commits WITH him. Never commit
  imgui.ini.
- Remove TEMP probes (kTinyPoints, kMinimalVS, SS_RENDER_*, [CLOSURE]) as
  slices close; slice 5 legacy-pressure deletion.
