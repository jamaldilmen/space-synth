# SPACE SYNTH TUBE — Handoff 2026-07-10 18:07:01
Branch `session-2026-06-30-honest-spacetime-friction`. **Show in 1 day.**
(Primary handoff is the auto-memory `space_synth_handoff_2026-07-10.md`; this is the git mirror.)

## LIVE BLOCKER — vertical SLICES at rest
Uncommitted Plummer spawn (`src/core/particles.cpp`) renders the star map as ~3–4 vertical white
sheets at rest/launch, no audio.
- Spawn positions PROVEN clean (isotropic sphere; angular probe = smooth bell all 3 axes). Slices
  are downstream, not the spawn.
- Top suspect: PM-gravity red-black SOR (ω=1.9, `poisson_sor`, renderer.mm:1587) ringing on the
  newly-concentrated core (grid 128³/±64, cellSize 1.0). NEW with concentration.
- First test (no rebuild): `SS_SOR_SWEEPS=200` env, or toggle PM gravity (bit10) off. If slices go,
  confirmed. Fix: raise `A_PLUMMER` (gentler core) / finer rest grid / more sweeps.

## Committed today
- `99dac49` home-pin rate-only (fixed onset jump; restored collisions)
- `3d0a691` star colour: heat pedestal removed (Lupton: colour ≠ brightness) — "cool"
- `1e68d35` spawn: filled centre r_inner 25→0 (killed the bright ring) — superseded by Plummer
- `10402fd` removed held-BH latch (star map returns after a note) — was masking every BH bug

## Uncommitted
`src/core/particles.cpp` (Plummer, carries the slice regression — leave until fixed).
Binary + `imgui.ini` always dirty. NEVER commit imgui.ini.

## Scale = Option C (agreed, not yet built)
Physics true-scale decides WHEN (geometric horizon r≤r_s, r_s=1.0 sim). Style decides HOW BIG:
anchor to supernova shape (ORBIT_R_CHLADNI=3). Add `BH_RS_VIS`, derive horizon/photon-ring/ISCO by
Kerr a=0.99 ratios (0.5705 / 0.5838 / 0.7272). Proposed BH_RS_VIS=0.1375 → horizon 2.6% of shape,
disk outer 1.0 (June-4 proportion), 10× disk span. Capture radius scales with BH_RS_VIS not r_s.

## Why no BH forms
bit1 central-SMBH OFF (correct); bit3 seed-merge OFF (mass fragments); bit7 seed-render OFF
(invisible); `M_BH_SEED=50` is a mass cutoff, violates the geometric-horizon canon (no r≤r_s test).
Live test: mod-menu enable seed-merge + seed-render, don't play a note, watch a dominant mass form.

## Scale facts (measured)
1 sim = 0.0117 AU; map radius 60 = 0.70 AU = 60 r_s of a 5.94e5 M☉ BH (accretion-disk scale, not
star-cluster). Stars ~30,000× too close → collisions 38× faster than crossings → the merge storm is
correct physics for this density. `kColdFactor=0.05` = 3.2% of circular v → cluster free-falls.

## Open quality items
starLum clip → Lupton asinh (white blobs, render.metal:729); Teff `5772*M^0.55` too red → Eker
(render.metal:666); pointSize cap 150 vs comment "40" (render.metal:476); dynamic_cap cylindrical
clamp = tube feel (particles.metal:2216). Science banked in memory `reference_stellar_render_sources`.

## Agenda (docs/agenda_2026-07-08.md)
A starmap: mostly done today (colour/centre/latch ✓, Plummer blocked by slices). B physics/BH: barely
started (M_BH_SEED cutoff is the canon fix). C perf: 100fps gate open. D: uiCollisions/uiBondNetwork
engine-permanent conflict STILL needs Jamal's ruling. E: imgui.ini hygiene, TEMP probes still in.
