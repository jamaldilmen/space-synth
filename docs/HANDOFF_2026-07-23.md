# HANDOFF 2026-07-23 (03:35) — play-cavity shape fixes committed; whiteout relocated to the RENDER

**Branch:** `session-2026-06-30-honest-spacetime-friction`. **Committed HEAD:** `540b8db`.
**Prior baseline:** `1bb9c70`. Everything below `540b8db` is uncommitted working tree.

## One-line state
Committed (`540b8db`) three grounded play-cavity CHANGES: proportionate depth, closed-can axial bound, low-C pAx. The axial-bound morph is demonstrably tamed. ⚠️ **The low-C eye is NOT fixed by Jamal's eyes** ("still an eye but okay for now") and two-equal-rings is NOT confirmed — these are improvements + mechanisms, not resolved bugs; the eye is still OPEN. The big pain — the **pause/settle whiteout + 5 FPS crash** — was chased in physics for hours but is now proven **render-side** (survives a physics freeze). Uncommitted experiments (recycle, density-gas, timewarp) below with honest verdicts.

## COMMITTED this session (`540b8db`) — verified on screen
- **Proportionate depth** `EIGEN_L = 2R` (was Jeans π²R ≈ 5:1 needle → read as an eye from every angle). particles.metal.
- **Axial bound** — the play clamp bounded XY but left Z FREE (an infinite tube), so eruptions/thermal/mode-leakage elongated the shape along the free axis into the eye over ~seconds ("launches correct, morphs into the eye"). Now `|z|` is bound to the cavity half-length = a closed can. Hit sculpt AND eigenmode identically (shared cavity) — that's how we knew it wasn't the play force.
- **pAx flat-disk fix** `pAx = 2 + (m+n)%3` (was `1 + …`). The chord-desync formula gave `pAx=1` exactly when `(m+n)%3==0` — which is low C (m=0,n=3). `pAx=1` = a single axial node = a flat disk = a thin line/eye edge-on → **that is why ONLY low C turned into an eye.** Now never 1: low C → two axial planes = two equal rings side by side. NOTE: Jamal says low C is "still an eye but okay for now" — the pAx fix helped but did not fully resolve; parked.

## UNCOMMITTED experiments this session (verdicts)
- **Rest-recycle** (particles.metal, `REST_RECYCLE=0.005`) — Jamal's reset loop (#1): dead/eaten matter (mass≈0) trickles back to its star-map home at REST, not just on play. VERDICT: valid loop, but **does NOT fix the whiteout** — measured `hole 0%`, i.e. nothing is eaten yet, so there's no dead matter to recycle. Unverified for its real job (needs a formed, eating BH). Keep or drop.
- **Density-gated gas** (render.metal — plumbed `cellCounts`+`SpatialHashUniforms` into `particle_vertex` at buffers 4/5, bound in renderer.mm at the main draw). Renders DENSE cells as flux-conserving gas. VERDICT: **did not help the settled BH** — the settled ring is *sparse* (a thin ring of individual dots), so there's no per-cell density to trigger the gaseous kernel. Density is the wrong signal for a sparse ring. Plumbing is correct and reusable; the gate (`smoothstep(5,30, cellCount)`) just never fires on the sparse ring.
- **speed-gated gas** (render.metal) — earlier attempt; wrong gate (settled ring is slow → never fires). Superseded; still valid for fast infall/spaghettification.
- **TIMEWARP FIX** (renderer.mm/.h + main.cpp) — **grounded regression fix, NOT yet visually confirmed by Jamal.** `computeStep` hardcoded `dt = 0.0165f`, throwing away `simDt = dt×timeWarp` from main.cpp → x2/x4/x8 did nothing. Fix: `dt = 0.0165f × impl_->timeWarpVal` (pinned base stays fixed frame-to-frame = stability intact per the energy-pump comment; scaled by warp = controls work). New `setTimeWarp()` setter, called from main.cpp; clock uses the same physics dt. **Commit only after Jamal confirms x2/x4/x8 move the sim.**

## THE WHITEOUT + 5 FPS — RELOCATED TO THE RENDER (key correction)
Hours were spent on physics (rest collapse, recycle). **The whiteout survives a physics pause** (SPACE skips `computeStep` entirely, yet it still whites out + crashes to 5 FPS mid-chladni-shape). Therefore it is NOT physics. Two render-side mechanisms:
1. **Whiteout = frame persistence + analytic streak sweep.** postfx.metal:372 `color = max(color, prev·trailDecay)` reads the previous frame back. The render streaks particles with an ANALYTIC orbital velocity (not stored motion), so even with frozen positions the streaks sweep and the trail buffer paints them toward white over ~3 s. (Max alone wouldn't runaway a static image — the analytic motion is what makes it sweep. VERIFY this next.)
2. **5 FPS = gas-splat overdraw.** Paused mid-shape, `envelopePhase` is frozen at play → all ~2M packed particles draw ×3-wide soft splats → massive fragment overdraw. Constant cost, not accumulating.
NEXT: instrument/confirm the trail-vs-analytic-streak theory (disable `trailDecay` when paused, or freeze the analytic streak phase when paused); and gate the gas-splat width down when the shape is static/dense to recover FPS.

## Physics facts measured this session (keep)
- **Rest collapse is LIVE matter, pre-BH.** At rest, live matter collapses to a sub-cell knot (`r50≈0.059`, one AMR fine-cell = 0.0625) with `M(<0.5)≈1.1e5`, all while `hole 0%` (no BH yet — horizon needs 2.97e5 within 0.5). So the dense core is infalling live matter, not eaten/dead.
- **Temp "runaway" is a red herring for the whiteout.** `[CLUSTER]` logs sim temp ~1e10–1e12 (the code's own "substrate-noise DEBT" at particles.metal:~2790), BUT the render CLAMPS temp everywhere it uses it (`heatK=clamp(temp,0,5)`, luminance→2.5, size→1). So temp cannot whiten the screen.

## OPEN LIST (Jamal's lifecycle vision is the through-line: gas → stars → mergers → BH → eaten → gas)
1. **Gaseous BH ring / spaghettification** — the settled ring is sparse popcorn/"ugly lines"; density-gate can't help a sparse ring. Needs a different approach (fewer, larger, soft kernels regardless of density? or actually concentrate more matter into the ring?). Jamal: "physically questionable, low res, cheap."
2. **Whiteout (render)** — persistence + analytic-streak sweep (see above).
3. **5 FPS on dense/paused shape** — gas-splat overdraw.
4. **Reset/lifecycle loop** — recycle in place (dead→home); needs the full cycle: eaten→gas ring, settle→gas→stars→mergers→BH.
5. **Timewarp fix** — confirm + commit.
6. **Fullscreen darker** — likely macOS colour-management (windowed vs fullscreen gamma path). Untouched.
7. **BH rotation not right**, **BH render "flawed with temp"**, **Doppler invisible**, **rest-drain/angular-momentum wall**, **lens hole-centering** — all still open from prior sessions.
8. **Sculpt vs eigenmode default** — unresolved. Sculpt shows the "correct" two-equal-rings but was the hollow-shell/FPS-eater; eigenmode is default. Decide once shapes are close.

## RITUALS (unchanged)
`bash package_macos.sh` (never bare make); verify bundle metallib/binary ≥ source timestamps BEFORE testing; "did nothing" → stale binary FIRST; `pkill -x SpaceSynth` between launches; I change / HE plays; his eyes are ground truth; ONE change per verdict; commit ONLY on explicit order.
